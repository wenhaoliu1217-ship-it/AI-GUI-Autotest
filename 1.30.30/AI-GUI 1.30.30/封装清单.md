# AI-GUI 1.30.00 冻结版本封装清单

更新日期：2026-07-22

## 补丁性质

本次变更是 `1.30.00` 正式冻结后的交付兼容性补丁，仅修正 Windows 一键运行与离线封装能力，不新增或修改业务功能、测试能力、执行策略及版本需求归属。

## 交付目录

| 路径 | 内容 | 当前实测大小 |
| --- | --- | ---: |
| `Windows一键运行/runtime/python` | 便携 Python 3.12.10 与固定应用依赖 | 127.58 MiB |
| `Windows一键运行/runtime/ms-playwright` | Playwright 1.49.1 的 Chromium、headless shell、FFmpeg、winldd | 510.60 MiB |
| `Windows一键运行/runtime/images/ai-gui-runner-1.30.00.tar` | 离线容器 Runner 镜像归档 | 757.30 MiB |
| `Windows一键运行/backend` | 已冻结后端及 Runner 定义 | 9.43 MiB |
| `Windows一键运行/dist` | 已冻结前端生产资源 | 0.60 MiB |
| `Windows一键运行/start.bat`、`start.ps1` | Windows 离线启动入口 | 已包含 |
| `Windows一键运行/stop.bat`、`stop.ps1` | 仅停止当前包服务的手动兜底入口 | 已包含 |
| `Windows一键运行/README-Windows.txt` | 一键运行说明及目录契约 | 已包含 |

目录大小按当前工作目录文件字节统计，最终 ZIP 压缩后大小以封装校验结果为准。

## 离线约束

- 启动器只使用 `runtime/python/python.exe`，不调用系统 Python，也不创建虚拟环境。
- `PLAYWRIGHT_BROWSERS_PATH` 固定指向包内 `runtime/ms-playwright`。
- 禁止在线运行 `pip install`、`playwright install` 和 `docker build`。
- Docker 中已有 `ai-gui-runner:1.30.00` 时直接复用；镜像不存在时只允许从包内 TAR 执行 `docker load`。
- Docker Desktop、WSL2 Linux Engine 与 Windows 虚拟化是系统级前提，不包含在 ZIP 内。
- Docker 不可用或交付文件不完整时明确失败，不降级为宿主进程执行测试。
- Uvicorn 子进程绑定启用 `KILL_ON_JOB_CLOSE` 的 Windows Job Object；关闭启动窗口时由系统回收本包服务进程树。
- `server.pid` 记录 PID、本包路径、便携 Python 路径和进程启动时间；正常退出自动清除。
- 启动前及 `stop.bat` 手动停止均严格核对同包 Python、启动时间和 Uvicorn 命令行，不按端口或进程名终止其他进程。

## 封装前复验

- [x] 便携 Python 报告版本 `3.12.10`。
- [x] Playwright 运行时包含 headed Chromium、headless shell、FFmpeg 和 winldd。
- [x] 当前完整目录通过 `start.bat` 启动，服务在 `http://127.0.0.1:8081/` 健康响应。
- [x] 删除 `ai-gui-runner:1.30.00` 后，从包内 TAR 离线恢复成功。
- [x] 恢复镜像 ID 前缀为 `9b379...`。
- [x] 启动脚本不存在在线安装或在线构建路径。
- [x] `start.ps1`、`stop.ps1` PowerShell 语法检查通过。
- [x] Windows Job Object C# 类型编译、子进程绑定及 `KILL_ON_JOB_CLOSE` 实际终止通过。
- [x] 无 PID 记录时 `stop.ps1` 安全返回，未扫描或停止其他进程。
- [x] 最终 ZIP 按本清单生成。
- [x] 最终 ZIP 文件大小记录在相邻 `.sha256` 文件和版本状态文档中。
- [x] 最终 ZIP SHA-256 记录在相邻 `.sha256` 文件和版本状态文档中。

## 最终 ZIP 校验

- 文件名：`AI-GUI 1.30.00.zip`
- 文件大小与 SHA-256：以压缩包相邻的 `AI-GUI 1.30.00.zip.sha256` 为唯一外部校验记录，避免压缩包自引用导致哈希变化。
- 封装日期：2026-07-22
## 1.30.30 当前封装状态

- 交付目录版本：`1.30.30`；仅允许修改该版本目录和根接续说明。
- Windows 运行包已包含当前源码后端、生产前端、Python 3.12.10、Playwright 1.49.1 Chromium 和 `ai-gui-runner:1.30.30` 离线 TAR。
- Runner 镜像 ID：`sha256:c73a94279ac7db5a2e601b8c3f5dd3817cd58ef6a580cca155fbd37c3de89ca3`。
- TAR SHA-256：`CD84234E87488396C9D9D8CBA23AB3AE7BC21E84993327A9D2C92120EEE49564`；runtime manifest 已通过逐文件 SHA-256 核对。
- 后端 unit `151 passed`、E2E `29 passed`、前端 `21 passed`、TypeScript 通过；干净临时目录启动/停止和端口释放通过。
- J01～J65 无企业账号、页面绑定和隔离交易沙箱，当前结果固定为 `325 blocked / 0 verified / unverified`；不得写成企业验收通过。
- 最终 ZIP 文件名、大小和外部 SHA-256 待本目录内容冻结后填写；在真实京东绑定前不得宣称产品验收完成。
- 最终 ZIP：`AI-GUI 1.30.30-final.zip`，大小 `1,925,546,440` bytes，SHA-256 `E40C0179060A526379111C732935F9E6683BEE500DF5B7565B26058CF645EE46`；外部校验记录位于版本目录同名 `.sha256` 文件。
