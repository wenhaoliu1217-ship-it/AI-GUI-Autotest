# AIGU-2.3 AI 接入版真实 GUI 自动化测试平台

更新日期：2026-07-22

## 重要变化

本版本已移除前端 Mock 结果生成器。系统现在通过本地 FastAPI 服务调用 Playwright Chromium，真正访问目标网站、执行审核后的步骤和断言，并为每个已执行步骤保存 PNG 截图。

- 未连接执行服务时，GUI 明确报错，不生成成功或失败结果。
- 已删除“制造一个失败结果”开关；最终状态来自浏览器实际执行。
- 无法解析的自然语言会返回错误或人工补充项，不会默认判定成功。
- “截图”按钮打开本次运行实际生成的 PNG 证据。
- 新增“AI 模型设置”入口，支持 OpenAI Responses API 和兼容 Chat Completions。
- AI Key 仅保留在当前网页内存并传给本机后端处理；不落盘、不进入浏览器存储、历史记录或报告，刷新页面即清除。
- 模型生成的计划必须经过封闭动作白名单和 Pydantic Schema 校验，并仍需人工审核后才能执行。
- 新增企业“项目接入”：项目配置、允许域名、运行限制和真实只读兼容性扫描。
- L1 项目可导入 Playwright `storageState`；Cookie/Origin 先做域名校验，再使用 Windows 当前用户 DPAPI 加密，明文不会写入磁盘、报告或前端响应。

## 项目接入与 L1 登录态

1. 点击左侧“项目接入”，填写项目名称、Base URL、允许域名和运行限制。
2. L0 用于公开／免登录页面；L1 用于需要已登录会话的业务页面。
3. L1 可选择由 Playwright 生成的 `storageState.json`，再点击“加密导入登录态”。
4. 系统只展示 Cookie 数量、Origin 数量、域名和过期状态，不展示 Cookie 值。
5. 点击“启动真实只读扫描”；扫描只导航和观察，不点击、不填写、不提交。
6. 选择该项目后新建并执行测试，真实浏览器会加载该项目的登录态和允许域名限制。

登录态使用 Windows DPAPI CurrentUser 加密，只能由导入它的同一 Windows 用户解密。复制到其他电脑或其他 Windows 账号后需要重新导入。产品内交互录制登录态尚未实现。

## 在网页中接入 AI

1. 启动平台后，点击左侧“AI 模型设置”。
2. OpenAI 用户保留“Responses API”和 `https://api.openai.com/v1`；兼容服务请选择“Chat Completions”并填写服务商地址。
3. 填写模型名称和重新生成、从未暴露的新 API Key。
4. 点击“测试模型连接”；成功后点击“使用 AI 新建测试”。
5. 输入自然语言流程，选择“AI 模型规划”，生成后逐步审核，再启动真实浏览器测试。

连接测试和 AI 规划会产生模型调用费用。页面不会保存配置，刷新后需重新填写。已经在聊天、截图或日志中公开过的 Key 必须撤销，不能继续使用。

## Windows 最快运行

1. 完整解压交付 ZIP，不要只复制 `start.bat`。
2. 安装并启动 Docker Desktop，等待 WSL2 Linux Engine 可用。
3. 打开 `Windows一键运行` 文件夹并双击 `start.bat`。
4. 启动器固定使用包内 Python 3.12.10 和 Playwright 1.49.1 Chromium，不要求安装系统 Python。
5. 已存在 `ai-gui-runner:1.30.00` 时直接复用；不存在时从包内离线镜像归档恢复。
6. 浏览器打开实际提示的本地地址；若 8080 已占用，启动器会在 8080～8090 中选择空闲端口。

启动窗口是本地服务的生命周期控制窗口。按 Enter 正常退出或直接关闭窗口时，Windows Job Object 会终止本包 Uvicorn 进程树并释放端口。若需要从另一个窗口停止服务，可双击 `Windows一键运行/stop.bat`；它只在 `server.pid` 中的包路径、便携 Python 路径、进程启动时间和 Uvicorn 命令行全部匹配时停止当前包服务，不会按端口或进程名称影响其他版本。

本交付包的启动过程严格离线，不会执行 `pip install`、`playwright install` 或 `docker build`。若 Python、Chromium 或 Runner 归档缺失，启动器会明确报错，不会从互联网修复，也不会降级到宿主进程执行测试。

便携运行时位于：

```text
Windows一键运行/runtime/
  python/                         # Python 3.12.10 与固定应用依赖
  ms-playwright/                  # Playwright 1.49.1 Chromium 运行时
  images/ai-gui-runner-1.30.00.tar
```

Docker Desktop、WSL2 和 Windows 虚拟化仍是系统级前提，不能作为普通文件随 ZIP 便携启动。本补丁只调整冻结版本的交付兼容性，不改变 `1.30.00` 的业务功能、执行策略或版本需求范围。

运行证据保存在：

```text
Windows一键运行/artifacts/<运行编号>/
  run.json
  events.jsonl
  report.html
  report.md
  trace.zip
  screenshots/*.png
```

## 自然语言格式

当前内置的是透明、确定性的中文规则规划器。推荐使用明确语句并用分号分隔：

```text
在“用户名”输入“admin”；
在“密码”输入“admin123”；
点击“登录”；
确认看到“客户管理”；
截图
```

复杂业务流程可在“AI 模型设置”中接入经企业批准的模型供应商。AI 只负责把文字转换为受约束计划，不会直接宣布测试成功；最终结果仍来自 Playwright 的真实操作和断言。

## 真实执行边界

- 只测试已获授权的 Web 测试环境。
- 首期执行器为 Chromium；不包含小程序或 Windows 桌面自动化。
- 验证码、MFA、SSO 和真实账号必须采用企业批准的测试方案。
- 目标页面发生跨域跳转时，执行器默认拒绝。
- 失败原因属于启发式提示，必须结合截图与 trace 人工复核。

## 公网部署说明

纯静态托管无法在云端启动 Chromium。公网部署必须同时部署 Python 执行服务到允许启动浏览器的 Windows/Linux 主机或容器，并在构建 GUI 时设置 `VITE_API_BASE_URL`。只部署 `dist` 会显示“执行服务未连接”，这是防止伪造结果的设计行为。

## 验证结果

- 2026-07-22 冻结版本交付兼容性复验：当前完整目录双击 `start.bat` 成功启动，服务在 `http://127.0.0.1:8081/` 返回健康响应。
- 便携 Python 实测为 3.12.10；包内 Playwright 固定为 1.49.1，并携带 headed Chromium、headless shell、FFmpeg 与 winldd。
- 删除本机 `ai-gui-runner:1.30.00` 后重新启动，启动器从 `runtime/images/ai-gui-runner-1.30.00.tar` 离线恢复镜像成功，恢复镜像 ID 前缀为 `9b379...`。
- 启动脚本已静态核对：不存在在线 `pip install`、`playwright install` 或 `docker build` 路径。
- 端口生命周期补丁已通过 PowerShell 语法检查及 Windows Job Object 编译、子进程绑定和 `KILL_ON_JOB_CLOSE` 实际终止验证；无 `server.pid` 时 `stop.ps1` 安全返回，不会扫描或终止其他进程。
- 最终交付 ZIP 的文件大小与 SHA-256：待最终封装完成后补录。

## 2026-07-23 开发中验证状态

- 当前版本仍在开发，尚未进入最终镜像重建和 ZIP 封装阶段。
- 后端完整 unit：`130 passed`。
- 前端：`19 passed`；TypeScript 类型检查通过。
- 真实 Chromium 定向：`4 passed`，覆盖电商行作用域、多窗口和 iframe。
- 已完成结构化电商门禁、资源台账、零残留、窗口／iframe、D 级人工接管、订单／库存／支付／退款状态机、只读后台轮询、双会话库存屏障和支付／退款回调幂等验证器。
- 尚缺文件工件 SHA-256、安全恢复、证据发布门禁和 65×5 控制台；因此不得封装或宣称 `1.30.30` 完成。
- 京东真实 J01～J65 均保持 `blocked/unverified`，本地测试结果不得替代公司账号和隔离交易沙箱验收。
- 文件工件当前仅完成项目级内容寻址登记、双端 SHA-256 和 GUI 列表；Runner 上传、下载捕获、容器只读挂载和报告证据仍在开发，尚不能计为完整文件闭环。
- 文件工件闭环现已完成：固定 Runner、Agent Runner、生成脚本、GUI、容器单次暂存、真实 Chromium 上传／下载和 SHA-256 报告均已接通。当前回归为后端 `133 passed`、真实 Chromium `5 passed`、前端 `19 passed`。

## 2026-07-23 故障安全恢复完成

- 只读动作遇到 HTTP 429、HTTP 5xx、断网或页面会话崩溃时，最多执行 3 次有界指数退避；页面崩溃时会在同一隔离 BrowserContext 中重建页面后再执行。
- 电商副作用动作的执行结果不明时默认禁止再次点击。只有同时声明结构化 commerce 元数据、幂等键引用和只读后台状态探针，并且探针证明仍处于动作前状态时，才允许重放 1 次。
- 后台已达到期望状态时，原动作视为已生效并以 `recovered_without_replay` 收口，不重复副作用；探针缺失、失败或状态含糊时，运行以 `side_effect_outcome_unknown / manual_reconciliation_required` 结束。
- 恢复证据记录每次尝试、429／5xx／断网／会话分类、退避时间、是否重试、幂等键 SHA-256、后台判断及未重放原因，不记录幂等键原文或后台完整响应。
- 固定 Runner、Agent Runner、生成的 Playwright TypeScript、GUI、JSON、Markdown 和 HTML 报告均采用同一恢复语义。
- 验证更新：后端完整 unit `141 passed`；恢复真实 Chromium `3 passed`；完整 E2E 首轮 `27 passed, 2 failed`，两项失败定位为报告渲染器既有 Canvas Markdown 缩进问题，修复后 Bridge 定向 `6 passed`；前端 `19 passed`；TypeScript 通过。
- 本项完成后下一固定优先项为：证据完整率、隐私泄漏、零残留和重复副作用发布门禁。当前仍不得封装，J01～J65 仍为 `blocked/unverified`。

## 2026-07-23 电商发布门禁完成

- 每次启用 CommerceProfile 的运行都会生成 `commerce-release-gate.json`，并把同一结果写入 `run.json`、GUI、Markdown 和 HTML 报告。
- 发布门禁固定检查四项：证据完整率不低于 98%；隐私泄漏 0；E2E 残留资源 0；重复资源引用和结果不明副作用 0。任一项不满足，运行以 `commerce_release_gate_failed` 结束，不能计作可发布验收。
- 证据完整率逐步骤核对动作前／后观察、动作前／后截图、恢复决策；上传／下载额外要求文件 SHA-256，后台状态证据存在时必须一致。
- 隐私检查覆盖手机号、身份证号、银行卡号和邮箱；门禁结果只保存泄漏类型、字段位置和命中值 SHA-256，不回写敏感原文。
- 验证更新：后端完整 unit `146 passed`；启用 CommerceProfile 的 429／503 恢复与发布门禁真实 Chromium `3 passed`；前端 `19 passed`；TypeScript 通过。
- 下一固定优先项为 65 场景 × 5 次产品化验收控制台。控制台完成前仍不得封装，J01～J65 继续保持 `blocked/unverified`。

## 2026-07-23 京东 65×5 验收控制台完成

- 管理端新增“电商验收”，固定读取 J01～J65、每场景 5 次和 325 次总分母，展示批次状态、六类尝试计数、七项门槛、每场景五次状态和阻塞依赖。
- 批次持久化支持建立、刷新、取消、恢复、失败重试、真实尝试登记和 HTML 报告下载。
- verified 结果必须关联真实 `run.json`；证据完整率、稳定回放、发布门禁、金额、零残留和零容忍事件由后端派生，前端提交的自报成功值不能成为验收结论。
- 当前真实目录验证结果：65 个场景、325 次尝试、325 blocked、0 verified、总体 unverified。金额和清理没有观测时均显示未通过。
- 验证：后端完整 unit `151 passed`，真实目录／批次定向 `5 passed`，前端 `20 passed`，TypeScript 通过。
- 下一步是完整技术回归与 Runner／离线交付重建。当前仍不得封装，京东真实页面绑定仍等待授权账号和隔离交易沙箱。

- 前端测试：6/6 通过。
- TypeScript 类型检查：通过。
- Python/API/Playwright 测试：22/22 通过。
- AI 接口测试：连接、密钥脱敏、结构化计划校验和目标地址防篡改均通过；未使用真实 Key。
- 真实成功路径：浏览器访问本地授权演示站，完成登录、页面断言和逐步截图。
- 真实失败路径：定位不存在的按钮，返回错误终态、失败分类和截图证据。
- GUI 联调：1440px 与 390px 均完成计划生成、审核、真实执行、报告和截图打开验证。
