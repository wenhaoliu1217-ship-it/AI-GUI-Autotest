# Cesium ion GUI 逐条验收记录（30 项）

- 执行入口：`http://127.0.0.1:8080/`
- 目标网站：`https://ion.cesium.com`
- 执行方式：网站识别和登录确认后，在 GUI 聊天框逐条发送任务。
- 判定规则：失败、阻塞、未执行均不算通过；失败项修复后只重测失败项。
- 安全边界：不记录个人资料、Cookie、令牌值或密钥；不实际发布、共享、删除、创建令牌或付费。

| 序号 | 小白用户任务 | 状态 | 运行编号/证据 | 失败原因与修复 |
|---|---|---|---|---|
| 01 | 检查保存的登录状态是否有效，能否进入受保护页面并看到账号已登录状态。 | 重测通过 | 首次 Run `20260725-200318-821efc10`；重测 Run `20260725-202822-c107a5c8`，3 个只读步骤完成，页面为 `Stories \| Cesium ion` | 首次失败原因：人工登录等待被 Agent 内部时限错误计入。已修复运行器等待计时并通过 18 项定向测试；用户重新登录后真实 GUI 重测通过。 |
| 02 | 检查登录后的主要导航入口、页面标题和当前选中状态是否一致。 | GUI 闭环通过；发现站点缺陷 | 首次 Run `20260725-202822-c107a5c8`；二次 Run `20260725-214158-cbb79b50`；最终 Run `20260725-223147-f2076772`。最终 Run 逐项核对 `Stories`、`My Assets`、`Asset Depot`、`Clips`、`Access Tokens`、`Usage`，10 次模型决策、9 个真实只读步骤。 | 首次失败：截图未授权给模型时连续截图至无进展上限；已增加导航 `href`、ARIA、`active/current/selected` 结构事实及截图防循环。二次失败：`wait_for` 在目标出现前错误要求唯一命中；已修复等待语义和显式时限，定向回归 `18 passed`。最终发现目标站点缺陷：点击 `Usage` 后 URL 为 `/usage`，但页面标题、主标题和活动导航仍为 `Access Tokens`；GUI 无权修改 Cesium 站点。 |
| 03 | 打开资产列表，检查加载状态、空状态、错误状态和主要操作入口是否清楚。 | 重测通过 | Run `20260726-040345-32469046`；进入 `My Assets`，以一次性只读临时关键词提交搜索，等待 5 秒稳定后明确显示 `No results found`；加载已完成、未见错误提示，`Add data`、搜索、类型筛选、日期排序和分页入口均清楚可见。最终 `status=passed`、`goal_status=achieved`、`runner_isolation.mode=spawn_process`，审批记录为空。 | 在此前危险词误判和动作超时修复基础上，继续修复了部分覆盖误报完成、非动作决策多余动作外壳、当前页 URL 前置条件、`role + text` 定位、资产页确定性导航、空状态只读探测、搜索只执行一次并按 Enter 提交、稳定等待以及证据不足误完成；定向回归 `30 passed`。本次真实 GUI 重测闭环通过；期间一次 AI 连接瞬时失败由内置重试自动恢复，未重新配置 AI。 |
| 04 | 在资产列表中使用搜索，只读确认结果与关键词是否一致。 | 重测通过 | 首次 Run `20260726-042335-76d84063`、首次修复后 Run `20260726-043601-7607fef3`、最终重测 Run `20260726-043916-4a00bfec`。最终在 `My Assets` 搜索 `Google Maps 2D`，稳定结果为 5 条资产，5 个名称均包含该关键词；`status=passed`、`goal_status=achieved`、审批记录为空、隔离方式为 `spawn_process`。 | 首次失败：兼容模型给只读搜索错误附带副作用业务对象字段，触发 `E2E_` 前缀 Schema 拦截；修复为仅对 Cesium 搜索/筛选控件清除矛盾副作用字段，普通填写和真实副作用继续失败关闭。第二次失败：合法 action 决策缺少非执行参数 `reason`；仅为 action 补明确兼容说明，完成/澄清/视觉决策仍不放宽。定向回归 `32 passed`，最终真实 GUI 重测通过。 |
| 05 | 使用资产类型筛选和排序，只读确认列表状态是否正确变化。 | 重测通过 | 初次 Run `20260726-044637-4b66e54a`；修复验证 Run `20260726-045720-f71d3e04`、`20260726-050633-44157b4b`、`20260726-052145-0bf34ff3`；最终全自动 Run `20260726-053136-d4b1b71b`。最终自动选择 `Type = 3D Tiles`，列表仅剩 2 条对应资产；自动点击 `Date added` 后等待稳定，顺序由 2023→2020 反转为 2020→2023，并保存稳定截图。最终 `status=passed`、`goal_status=achieved`、审批记录为空、隔离方式为 `spawn_process`。 | 初次问题：明确任务在筛选后错误询问用户“当前高亮哪个入口”；补充基于 DOM 的 `3D Tiles` 确定性筛选与 `Date added` 确定性排序。随后发现排序即时截图仍在转圈，补充排序后自动等待 5 秒并截图。全自动重测又暴露 Cesium 启动页触发通用无进展 5 次上限，改为仅对系统生成的 Cesium 只读启动等待豁免通用计数，10 秒一轮、独立最多 6 轮；普通页面防循环不变。定向回归 `36 passed`，最终无需人工补充即可闭环。 |
| 06 | 打开一个已有资产详情，只读检查名称、类型、状态和元数据。 | 重测通过 | 最终 Run `20260726-060128-9175194f`。自动进入 `My Assets` 并打开 `Google Maps 2D Contour` 详情；确认类型 `Imagery`、日期 `Sep 30 2025 3:54 AM`、ID `3830186`、描述、标签、代码和地图预览均已加载。最终 `status=passed`、`goal_status=achieved`，审批记录为空、隔离方式为 `spawn_process`。 | 此前失败表现包括 Cesium 启动画面慢加载触发通用无进展上限，以及打开详情后在预览和元数据稳定前误判完成。已增加 Cesium 启动画面独立慢加载等待、详情目标确定性点击、详情稳定等待与截图，并收紧“详情未打开/证据不足”类完成判定；定向回归 `38 passed`，真实 GUI 重测通过，未修改、删除或创建资产。 |
| 07 | 检查已有资产的预览入口和预览加载反馈，不修改资产。 | 重测通过 | 首次 Run `20260726-061302-2ad06053`；线路/Schema 复测 Run `20260726-062633-c20c96c1`；最终 Run `20260726-064301-241f2b18`。最终自动打开 `Google Photorealistic 3D Tiles`（ID `2275207`），等待预览稳定后真实截图显示带纹理的 3D 地球、`View Home` 和全屏控制；5 个只读步骤、6 次模型决策，最终 `status=passed`、`goal_status=achieved`、审批记录为空、隔离方式为 `spawn_process`。 | 首次失败：安全检查把说明中的“**不修改资产**”误命中禁令“修改资产”；已改为只检查真实动作目标和结构化定位，真实修改按钮仍失败关闭。二次失败：兼容模型为只读打开预览错误附带副作用业务对象字段，触发 `E2E_` Schema 拦截；已仅对 Cesium 明确只读的预览/详情定位清除矛盾字段。另补充确定性预览闭环：固定打开已有 3D Tiles、等待 5 秒，并以资产标题、预览控制和可视表面共同判定加载，禁止虚构不存在的 `Preview` 按钮。期间 AI 线路一次瞬时失败由自动重试恢复；定向回归 `43 passed`，最终真实 GUI 重测通过。 |
| 08 | 打开上传资产入口，检查表单、必填提示和取消返回，不提交文件。 | 重测通过 | 初次 Run `20260726-064939-a63663fb`；分类修复 Run `20260726-065602-6dc21487`；导航循环 Run `20260726-070629-caf3a715`；网络恢复验证 Run `20260726-071650-6994328d`；最终 Run `20260726-072111-32de85c8`。最终真实进入 `/addasset`，稳定显示 `Add files`、S3、Azure、Sketchfab、格式帮助和 `Cancel`；未选择文件，点击 `Cancel` 返回 `/assets`。最终 5 个只读步骤、6 次模型决策，`status=passed`、`goal_status=achieved`、审批记录为空、隔离方式为 `spawn_process`。 | 依次修复四类 GUI 缺陷：未来清理说明中的 `delete` 被误当当前删除并要求确认；兼容模型漏填只读入口的 Cesium 副作用等级；`/addasset` 被通用资产导航强制带回列表形成往返循环；首次导航 `ERR_CONNECTION_CLOSED` 后在 `chrome-error://` 上执行相对路径导致安全错误。现已排除清理说明误判，为 `Add data`/`Cancel` 补只读分类，建立“表单稳定取证→Cancel→返回完成”的确定性闭环，并在错误页将相对恢复导航约束到已授权目标源。定向回归 `45 passed`，最终真实 GUI 重测通过，未选择或提交文件、未创建资产。 |
| 09 | 检查上传页面展示的支持格式、来源选项或帮助信息。 | 待执行 |  |  |
| 10 | 在未填写必填内容时尝试继续，确认 GUI 会先请求批准且网站会安全校验；不创建资产。 | 待执行 |  |  |
| 11 | 检查不同资产类型或分类入口是否可识别且命名清楚。 | 待执行 |  |  |
| 12 | 检查 3D Tiles 相关入口、列表或详情信息是否可访问。 | 待执行 |  |  |
| 13 | 检查影像相关入口、列表或详情信息是否可访问。 | 待执行 |  |  |
| 14 | 检查地形相关入口、列表或详情信息是否可访问。 | 待执行 |  |  |
| 15 | 打开 Stories 列表，检查加载状态、列表内容和主要操作入口。 | 待执行 |  |  |
| 16 | 打开一个已有 Story，只读检查标题、编辑区或查看区是否正常加载。 | 待执行 |  |  |
| 17 | 打开新建 Story 入口，检查初始表单和取消返回，不创建 Story。 | 待执行 |  |  |
| 18 | 检查 Story 的预览或演示入口是否可见，不发布内容。 | 待执行 |  |  |
| 19 | 检查分享和发布入口是否存在，并确认执行前会请求用户批准；不实际分享或发布。 | 待执行 |  |  |
| 20 | 打开账号区域，只读检查账号页面结构，不记录个人资料内容。 | 待执行 |  |  |
| 21 | 打开访问令牌列表，确认令牌值不会被 GUI 证据明文记录。 | 待执行 |  |  |
| 22 | 检查创建令牌入口，确认执行前必须请求批准；不创建令牌。 | 待执行 |  |  |
| 23 | 只读检查令牌权限或作用域说明，不显示、复制或修改令牌值。 | 待执行 |  |  |
| 24 | 打开用量或账单页面，只读检查当前用量和页面状态，不进入付款。 | 待执行 |  |  |
| 25 | 检查套餐、额度或限制说明是否清楚，不更改订阅。 | 待执行 |  |  |
| 26 | 检查后台处理或异步任务的状态、进度、成功和失败反馈入口。 | 待执行 |  |  |
| 27 | 检查通知、帮助或错误反馈入口是否可访问且不会遮挡主要操作。 | 待执行 |  |  |
| 28 | 刷新受保护页面，确认登录状态和当前页面可以恢复。 | 待执行 |  |  |
| 29 | 从站内导航再次进入受保护页面，确认不会错误回到登录页。 | 待执行 |  |  |
| 30 | 检查账号菜单中的退出入口，确认 GUI 不会未经批准自动退出；不执行退出。 | 待执行 |  |  |

## 汇总

- GUI 闭环通过：8
- 失败：0
- 阻塞：0
- 待执行：22
- 失败项重测通过：8
- 发现目标网站缺陷：1
