# AI-GUI 1.30.10 开发版本与企业需求状态

> 当前权威开发目录：`AI-GUI 1.30.10`。
> 直接复制基线：已冻结的 `AI-GUI 1.30.00`，完整复制日期为 2026-07-21。
> 发布边界：`1.30.10` 负责 ER-07、GAEALaViC／首个企业测试网站适配、授权数据与角色矩阵、30 场景×5 次重复基准及企业级验收；ER-08 和第二个非 Cesium 业务网站验证继续归属 `1.30.20`。

## 0. 版本目录规则

- `1.25.00` 和 `1.30.00` 均已冻结；`1.30.10` 是 GAEALaViC 的冻结现场交付基线，不再直接加入业务功能改动。
- GAEALaViC 现场反馈必须先完整复制 `.10` 为 `.11`，再只修改 `.11`；后续依次复制为 `.12/.13`。其他网站分支不得复制本版本，例如 `1.30.20` 必须从通用母版 `1.30.00` 新建。
- 每轮开发完成后必须同步更新版本状态说明和根目录接续说明；不得只改显示版本号。
- 本目录及子目录内的 AI 必须先遵循版本根 `AGENTS.md` 和 `1.30.10_GAEALaViC专用改动边界与迭代规则.md`。

## 0.1 `1.30.10` 初始化基线与当前主线

- `1.30.10` 已完整继承 `1.30.00` 的 FR-01～FR-11 通用单用户能力、Docker 隔离、Bridge／Canvas 证据与 Finding 分类；继承基线为后端 `118 passed`、前端 `18 passed`、TypeScript 与生产构建通过。
- 当前 P0 主线不是继续修改通用 FR，而是依据独立网站测试指导补齐 ER-07 和 GAEALaViC／首站适配：认证与角色矩阵、目标站定位／复杂组件、上传下载、异步状态、测试数据生命周期、Cesium 业务语义和企业验收。
- 企业目标站依赖必须来自完整测试指导和授权环境，不根据 GUI 现有能力反向删减指导书，也不得猜测租户、角色、固定数据、状态机、文件样例、下载格式或清理规则。
- `1.30.00` 自复制完成后保持只读；所有后续代码、测试、构建、运行证据和版本文档只写入 `1.30.10`。

## 1. 相较 1.20.00 的主要更新

### 1.1 项目接入与配置

- 增加 Project、Environment、Scenario 和审计记录的本地持久化。
- 增加 L0～L3 接入等级、允许域名、禁止动作、最大步骤、总时长和模型调用上限。
- 增加 Environment 创建／选择／编辑，支持普通变量、密钥引用、网络忽略、Viewport、设备缩放、工件保留字段和 App Bridge 配置。
- 增加 Scenario 创建／选择／编辑，支持目标、测试数据、预期结果和禁止动作；敏感测试数据必须使用密钥引用。
- 增加 storageState 安全导入、域名／过期校验、Windows DPAPI 加密和交互登录录制。

### 1.2 应用兼容性扫描

- 从基础单页观察升级为最多 6 个允许域名内安全 GET 页面的只读扫描。
- 扫描不点击按钮、不填写、不提交，不自动访问危险语义入口或第三方域名。
- 识别 DOM／ARIA、稳定属性、无名称控件、重复 ID、Canvas／WebGL、iframe、Shadow DOM、复杂编辑器和文件上传。
- 识别登录、验证码／MFA、SPA、Fetch／XHR、loading、Console、页面异常、失败请求和 HTTP 4xx／5xx。
- 输出建议接入等级、稳定／视觉／自适应／人工区域、主要导航和推荐配置。
- 扫描后自动创建继承项目禁止动作的安全示例场景。

### 1.3 执行架构与 AI

- 增加持久 Run Orchestrator、后台执行、状态轮询、取消请求、异常恢复和总时长限制。
- 增加 Observation→Planner→单动作→再 Observation 的逐步 DOM Agent。
- 强制最大步骤、最大模型调用、禁止动作和连续三次无进展停止。
- 增加可选截图视觉适配器、Canvas 相对坐标、置信度门槛和动作后状态变化验证。
- 增加 `visual_click`／`bridge_click` 数据与执行模型，但尚未完成通用 Computer Use 和完整 Cesium Bridge。

### 1.4 证据、问题与审核

- 每步采集动作前后截图、URL、标题、DOM／Accessibility、Console、页面异常和失败请求。
- 保存 Trace、运行轨迹、环境／场景来源、模型名称、Token、耗时、决策理由和可选成本。
- 生成结构化 Finding，区分事实和推断，并记录严重度、置信度、实际／预期、复现步骤和证据。
- 支持 Finding 标题／严重度／预期编辑、确认／待审／驳回。
- 支持回归路径保留、恢复、编辑和移除；原始计划保持不可变，审核计划独立保存。

### 1.5 测试生成与结果管理

- 生成 Playwright TypeScript，优先语义 locator，并标注 A～D 稳定性和 CI 建议。
- 支持源码预览／编辑／复制／修订／下载；D 级步骤显式 `test.skip`，不伪造可执行能力。
- 报告显示场景目标、目标完成状态、原始执行状态、审核终态、环境／场景追溯和总耗时。
- 支持 JSON／HTML 报告下载、稳定／自适应入口门禁、运行停止和历史报告选择／全选／批量删除。

## 2. 已完成的企业需求（单用户 MVP 范围）

| 需求 | 状态 | 当前完成内容 |
| --- | --- | --- |
| FR-01 项目与测试环境配置 | 完成 | 项目／环境创建、编辑、持久化、密钥引用、网络／Viewport／Bridge 配置和运行关联 |
| FR-02 应用接入与兼容性扫描 | 完成 | 安全多页只读扫描、接入等级、区域分类、推荐配置和自动示例场景 |
| FR-03 自然语言测试场景 | 完成 | 场景库、目标／数据／预期／禁止动作、敏感引用和运行追溯 |
| FR-07 人工审核 | 完成 | Finding 审核、路径编辑、不可变原计划、修改历史和重新编译 |
| FR-08 Playwright 测试生成 | 完成 | 语义代码、A～D、D 级人工说明、源码修订和下载 |
| FR-10 运行结果页面 | 完成 | 目标状态、证据、审核终态、下载、运行停止和历史批量删除 |

上述“完成”限定在 `1.30.00` 的通用单用户产品范围；企业目标站 Definition of Done 在 `1.30.10` 执行。

## 3. 仍未完成的企业需求

| 需求 | 当前状态 | 主要剩余项 |
| --- | --- | --- |
| FR-04 AI 探索执行 | **`1.30.00` 功能范围完成** | 目标优先、业务上下文、逐步 DOM／视觉 Agent、通用视觉手势、Bridge v1 五项契约、最小 Cesium 参考适配器、语义状态验证和动作前稳定性均已闭环 |
| FR-05 运行证据采集 | **`1.30.00` 功能范围完成** | DOM／视觉／Bridge 动作前后截图、页面事实、运行时异常、Trace、模型记录，以及 Canvas 场景、相机／图层／实体／选中／加载状态和失败阶段均已形成单步证据链 |
| FR-06 问题发现与结构化报告 | `1.30.00` 功能范围完成 | 通用页面诊断、Canvas／Bridge 七类确定性分类、事实／推断分离、截图与证据时间线已完成；企业站分类基准在本版本验证 |
| FR-09 稳定与自适应回放 | `1.30.00` 功能范围完成 | stable 保持固定计划且不调用模型；adaptive 要求当次 AI 设置并按当前截图重新定位，已有坐标变化与 Docker 实证；企业目标环境和重复基准转入 `1.30.10` |
| FR-11 安全控制 | 当前单用户 MVP 范围完成 | 默认私网阻断、域名策略、危险确认、工件生命周期、删除审计、截图隐私和 Docker 隔离 Runner 均已实现并取得真实容器证据 |

## 4. 企业负责人补充需求 `ER-01～ER-09` 状态

| 编号 | 补充需求 | `1.30.00` 状态／版本归属 |
| --- | --- | --- |
| ER-01 | URL 优先、尽量零改造接入 | 已完成通用能力：项目、兼容性扫描、L0／L1 黑盒执行和分级接入建议 |
| ER-02 | 顶层业务目标输入 | 已完成：默认输入业务目标和期望结果，不要求先写点击步骤 |
| ER-03 | AI 自主分解和动态决策 | 已完成：Observation→单动作→再观察，含 DOM 与通用视觉 fallback |
| ER-04 | 精简场景输入页面 | 已完成：角色、前置条件进入高级设置且继续影响 Planner |
| ER-05 | 陌生垂直业务理解 | **`1.30.00` 配置范围完成**：业务说明、术语、对象、状态、示例、操作边界、结构化允许操作、禁止操作、Bridge 能力清单和 Bridge 语义目标均可配置；实际 Bridge 执行适配随 ER-06 继续 |
| ER-06 | Cesium 仿真应用支持 | **`1.30.00` 通用能力完成**：DOM／Canvas Adapter、App Bridge、场景证据、动作前稳定性和语义 Finding 已具备；本版本执行目标站适配与验收 |
| ER-07 | Cesium 官方站及安全公共站验证 | **`1.30.10` 当前主线**：与 GAEALaViC／首个企业测试网站适配和企业验收联合完成 |
| ER-08 | 非 Cesium 业务站通用性验证 | **归属 `1.30.20`**，不阻塞 `1.30.00` |
| ER-09 | 每日代码与进度同步 | 项目流程要求，持续执行；外部推送和比赛平台发布仍需用户明确发起，不作为 GUI 功能模块 |

企业目标站至少 30 个固定场景、同版本固定数据重复 5 次及企业指标门槛统一归属 `1.30.10`。ER-07 归属 `1.30.10`，ER-08 归属 `1.30.20`，均不阻塞 `1.30.00`。

## 5. 当前验证基线

- 后端完整回归：`128 passed`（显式限定 `1.30.10\源码\backend\tests`，未收集冻结版本）。
- 前端：`21 passed`。
- TypeScript：通过。
- 生产构建：通过。
- FR-02 最终真实运行：`20260720-233536-662b87fb`，`passed／achieved`。
- 375×812 兼容性报告无页面横向溢出、控件越界或控制台错误。
- FR-11 容器成功运行：`20260721-163358-cbec1fad`；交付包成功运行：`20260721-165401-c9e894f5`；强停运行：`20260721-164841-0b5cefb5`。
- FR-04／FR-09 真实 adaptive 容器运行：`20260721-183446-e2727205`，`passed／agent_goal_completed`，动作后视觉验证通过，API Key 未持久化。
- 当前生产资源：`index-D-3EGqou.js`、`index-ePCDFdM0.css`；源码、一键运行和构建成品 5 个文件逐项 SHA-256 一致。

## 6. 下一步边界

- 企业新增要求使顶层目标动态 Agent 和 Cesium 成为核心；FR-11 完成后按既定分支恢复此前暂停的 FR-04，并与 FR-09 联合推进。
- FR-11 Docker 隔离及 FR-09 真实自适应找回已取得实证；当前下一项继续 FR-04／ER-06，完成可复用 Cesium／Canvas Adapter／Bridge 分层和动作前稳定性，再收口 FR-05／FR-06，并完成 ER-07 公共 Cesium 与 ER-08 非 Cesium 公共站验证。
- GAEALaViC 网站适配不进入 `1.30.00`；它归属 `1.30.00` 完整验收后按版本复制规则建立的 `1.30.10`。
- 若恢复 FR-04，应先做目标优先 UX 和业务上下文包，再补通用视觉／Cesium 适配与真实基准。
- `1.30.00` 是否完成只按通用能力与本地功能验收判断；企业目标站、30×5 重复基准和企业级 Definition of Done 不再作为其发布阻塞项。

## 7. 2026-07-21 第一批更新：FR-05／FR-06 通用证据与问题检测

- Observation 新增结构化页面健康信息、采集时间和待复核页面信号；浏览器侧可识别交互控件中心点遮挡、可见控件禁用指针事件、明显文本裁剪和加载完成后的空白页。
- Finding 新增动作前／动作后证据时间线，关联时间、页面、截图和当次日志；运行时异常同时携带动作前后截图，不再只挂单张后截图。
- HTTP `401／403`、其他 `4xx`、`5xx`、网络连接／DNS／超时请求分别标记为认证权限、客户端、服务端和网络时序失败；分类仍是待人工审核的事实标签，不冒充确定性根因。
- Playwright 动作超时明确描述为可能长时间无响应；逐步 Agent 尾部连续 3 次及以上无进展时生成 `unresponsive` Finding，并保存各步截图与时间线。
- 管理端运行步骤展示页面诊断数量和具体信号；Finding 审核卡展示证据时间线。离线 `report.html` 和 `report.md` 同步包含结构化问题及时间线。
- 页面信号只报告新出现的问题；同一遮挡／截断状态在后续步骤持续存在时不会重复生成 Finding。
- 新增真实 Chromium 页面诊断测试、Finding 分类／时间线／连续无进展测试和离线报告测试。
- 真实验证：后端 `57 passed`，前端 `14 passed`，TypeScript 通过，生产构建通过；目标诊断测试 `4 passed`。
- 交付同步：`源码\dist`、`Windows一键运行\dist`、`构建成品` 的入口、JS、CSS 哈希一致；4 个后端运行文件已同步到一键运行版。
- 版本复制修复：`Windows一键运行\start.ps1` 现在校验 `gui_agent` 必须来自当前 `backend`，否则自动重新执行本地可编辑安装，避免误加载冻结的 `1.25.00`。实际启动达到 `Ready: http://127.0.0.1:8081/`，导入路径确认位于 `1.30.00\AI-GUI 1.30.00\Windows一键运行\backend`。
- 本轮完成的是接续说明中 FR-05／FR-06 可独立实施的通用收口；Canvas Bridge 状态和 Canvas 语义检测仍随 FR-04 恢复后联合验收，截图像素级隐私处理留在 FR-11 安全闭环。
- 下一项进入 FR-11：优先实现默认私网／链路本地／DNS 重绑定阻断与受控本地测试例外，再推进危险动作确认、自动工件清理、运行删除审计和截图隐私策略。

## 8. 2026-07-21 第二批更新：FR-11 网络安全边界

- 项目配置新增 `allowPrivateNetwork`，默认 `false`；前端使用复选框显式开启“受控私网／本机目标”，创建、更新、持久化和审计均保存该字段。
- 默认阻断 IPv4 RFC1918、IPv6 ULA、回环、链路本地、CGNAT、未指定和组播地址；明确允许受控私网后才可访问本地或企业内网测试目标。
- 保留企业代理兼容性：本机公网代理把 `example.com` 映射到 `198.18.0.0/15` 基准网段，策略不会把该代理映射误判成 RFC1918 私网。
- 域名每次请求前重新解析并记录公网／受限网络范围；同一域名在运行中发生公网与私网范围切换时，按疑似 DNS 重绑定拒绝。
- Playwright Context 对导航和所有 HTTP(S) 子资源安装请求级守卫：导航同时检查域名白名单和网络范围，子资源至少检查网络范围，避免页面通过 Fetch、图片或脚本请求探测内网。
- 扫描器、交互登录录制、固定执行、逐步 Agent 和回放统一使用同一策略；安全拒绝写入 `network_request_blocked` 事件，导航失败返回明确安全原因。
- URL 同时拒绝非 HTTP(S) 协议和含用户名／密码的地址。
- 真实安全验收项目：`project-c8a4346f09`。默认访问 `127.0.0.1` 返回 `422`；项目更新开启例外后，运行 `20260721-115152-10e3f133` 为 `passed／achieved`，审计字段为 `allowPrivateNetwork`。
- 验证结果：安全／API／扫描目标回归 `38 passed`，API 真实浏览器路径 `3 passed`，后端完整回归 `69 passed`，前端 `14 passed`，TypeScript 和生产构建通过。
- 本子项完成后 FR-11 仍只是部分完成。下一项实现危险动作 `pending_confirmation` 状态转换和人工批准／拒绝审计；隔离 Runner、自动工件清理、运行删除审计和截图像素级隐私仍未完成。

## 9. 2026-07-21 第三批更新：FR-11 危险动作人工确认

- 新增统一危险动作匹配器，保守内置删除、退款、支付／付款、提交订单／生产表单、发布和邀请等中英文规则。该默认是工程安全值，不代表企业已经确认最终规则。
- 项目／场景 `forbiddenActions` 保持最高优先级并直接拒绝；未被明确禁止但命中内置危险规则的动作进入 `pending_confirmation`，不会直接执行。
- 固定计划和逐步 Agent 在实际 Playwright 动作前暂停；动作前截图已经采集，批准后才继续，拒绝后步骤保存为 `skipped`，运行以 `dangerous_action_rejected` 结束。
- Orchestrator 为每次请求生成独立确认编号，绑定运行编号、步骤、动作、目标和命中规则；错误编号、重复编号、已结束运行或服务重启后的确认均不能使用。
- 新增 `POST /api/runs/{run_id}/confirmation`，只接受 `approved／rejected` 和当前活动确认编号；取消待确认运行会唤醒线程并按用户取消结束，不会悬挂浏览器。
- 确认历史保存请求时间、决定时间、操作者、步骤、目标、规则和决定，进入 `run.json`、管理端运行页、离线 HTML／Markdown 报告。
- 危险回放同样进入后台确认；非危险回放保持原行为。回放前重新加载当前项目／场景禁止策略，不能使用旧计划绕过新策略。
- 前端运行页增加危险动作确认区、单次批准和拒绝按钮；真正的危险确认状态与 Finding `pending_review` 已分离，避免报告待审触发运行轮询。
- 新项目不再预填“删除／支付”为永久禁止项，交由确认状态机处理；用户显式填写的禁止动作仍硬拒绝。历史项目配置保持不变。
- 自动验证：固定计划批准／拒绝、Agent 拒绝、一次性编号、错误／重复编号、确认报告和真实 API＋Chromium 流程均通过。后端完整回归 `75 passed`，前端 `15 passed`，TypeScript 与生产构建通过。
- 一键运行入口达到 `Ready: http://127.0.0.1:8082/`；最新产物为 `index-CwwU0hlX.js`、`index-D9qt70XW.css`。
- FR-11 仍为部分完成。下一项实现 `artifactRetentionDays` 自动清理和运行删除独立审计，再推进截图像素级隐私与隔离 Runner。

## 10. 2026-07-21 第四批更新：FR-11 工件生命周期与独立删除审计

- 新增统一 `ArtifactLifecycle`，按每次运行保存的 `artifactRetentionDays` 判断到期时间；历史列表加载时自动执行保留策略，也提供显式 `POST /api/runs/cleanup` 命令。
- `queued`、`running`、`pending_confirmation` 三种活动状态始终受保护，即使保留期已过也不会被自动或手动删除。
- 缺失或非法保留期按 30 天处理；合法值限制到 1～365 天。到期判断优先使用运行结束时间，旧记录缺少有效结束时间时才回退目录修改时间。
- 手动批量删除会在删除任何目录前校验整批运行编号、存在性、终态和最终报告；任一项失败则整批不执行，保留目录 `_audit` 永远不可删除。
- 手动删除和自动清理统一写入 `artifacts/_audit/run-deletions.jsonl`。审计文件位于各运行目录之外，记录审计编号、时间、动作、操作者、原因、运行状态、结束时间、保留期、文件数、字节数、项目和环境，因此删除运行后审计仍保留。
- 新增 `GET /api/runs/deletion-audit` 下载独立 JSON 审计；历史页增加“执行保留策略”和“下载删除审计”，手动删除请求记录固定本地操作者。
- `ArtifactManager.finalize()` 统一调用当前离线报告渲染器，确保此前已完成的 Finding 时间线和危险确认历史确实进入生成的 HTML／Markdown，而不是落到旧重复实现。
- 自动验证：生命周期和 API 目标回归 `34 passed`；后端完整回归 `87 passed`；前端 `16 passed`；TypeScript 和生产构建通过。
- 当前资源为 `index-B4eDSh5k.js`、`index-D9qt70XW.css`；`源码\dist`、`Windows一键运行\dist`、`构建成品` 的入口、JS、CSS 和服务端 bundle SHA-256 一致，相关后端文件已同步到一键运行版。
- 一键运行验证达到 `Ready: http://127.0.0.1:8081/` 并自动退出。FR-11 仍为部分完成；下一项处理截图像素级隐私，之后实现隔离 Runner。产品整体仍未满足 Definition of Done，不得声明 `1.30.00` 已正式发布。

## 11. 2026-07-21 第五批更新：FR-11 截图像素级隐私

- 所有固定执行和逐步 Agent 证据截图在浏览器捕获瞬间应用不透明 `#111` 像素遮罩，PNG 落盘前敏感区域已不可见；截图完成后立即移除临时遮罩，不改变被测页面持久状态。
- 默认遮罩密码输入框、当前／新密码自动填充框，以及 `data-sensitive="true"`、`data-private="true"` 标记区域；环境配置新增 `screenshotMaskSelectors`，可用业务 CSS 选择器追加客户名称、账号、地图区域等敏感范围。
- 遮罩遍历当前页面的所有已附加 frame；单个 frame 不可访问或自定义 CSS 选择器非法时不会导致证据链失败，非法选择器和实际遮罩数量写入结构化截图隐私事件。
- CDP 主截图和 Playwright fallback 截图共享同一遮罩入口；视觉 fallback 读取的也是已经脱敏落盘的截图，不会向模型发送原始敏感像素。
- Playwright Trace 保留结构化快照和事件，但关闭未经过遮罩的内嵌截图；运行事件明确记录 `trace_pixel_privacy_enabled`，避免单独 PNG 已脱敏而 Trace 仍携带原始像素。
- 管理端环境配置增加“截图隐私遮罩 CSS 选择器”多行字段，创建、更新、持久化和环境修改审计均包含该配置；旧环境缺少字段时按空的附加选择器加载，默认遮罩仍生效。
- 新增真实 Chromium 像素验收：读取生成 PNG 的密码框、自定义敏感区域和公开区域像素，前两者为 `[17,17,17,255]`，公开区域保持 `[255,255,255,255]`；同时验证截图后遮罩节点已清除和非法选择器容错。
- 自动验证：截图隐私与 API 目标回归 `25 passed`，后端完整回归 `89 passed`，前端 `16 passed`，TypeScript 和生产构建通过。
- 当前资源为 `index-Dnn5eMbp.js`、`index-D9qt70XW.css`；三套前端产物和相关一键运行后端文件已同步。一键运行达到 `Ready: http://127.0.0.1:8081/` 并自动退出。
- FR-11 当前只剩隔离 Runner。产品仍有 FR-04／FR-09、Canvas／Cesium、企业目标站基准和 Definition of Done 等未完成项，不能声明 `1.30.00` 已正式发布。

## 12. 2026-07-21 第六批更新：FR-11 Windows 本地隔离 Runner

- 默认 `run_plan` 不再在 API 进程内的后台线程执行；固定计划、逐步 Agent、同步执行、后台执行和稳定／自适应回放统一进入 Windows `spawn` 子进程。显式注入测试 runner 时保留线程兼容模式，不影响产品默认路径。
- 每次运行使用独立工作目录和 `_runner_tmp` 临时目录；浏览器下载被禁用，Service Worker 被阻断，导航继续只允许 HTTP(S)，避免通过文件协议或下载旁路写入任意路径。
- 子进程环境变量改为最小白名单：只保留 Playwright／Windows 启动必需项、已审核计划明确引用的 `${...}` 变量，以及 `value_from_secret`／Environment `secretRefs` 指定的密钥名称；无关主机 Token 不会继承到 Runner。
- Windows Job Object 绑定 Runner 和 Chromium 进程树，启用进程树随 Job 关闭终止、未处理异常终止和 `2048 MB` Job 内存上限。父进程记录 PID、Job 绑定结果、工作／临时目录、网络策略和是否强制终止。
- Orchestrator 在父进程独立执行总时限；用户取消先给 8 秒安全关闭窗口，超时或无响应后强制终止整个 Job 进程树。服务重启、子进程异常退出和资源超限都有明确终态，不依赖 Runner 自己上报。
- 父子进程只通过内存 IPC 传递计划、配置、进度和一次性危险确认决定；API Key、storageState 和运行密钥不写入中间配置文件。确认编号仍绑定单次运行／步骤，批准与拒绝历史进入最终报告。
- HTTP 请求、WebSocket 握手、DNS 范围切换和私网边界统一受策略保护；WebSocket 额外强制项目允许域名。浏览器渲染进程继续使用 Chromium 自带沙箱。
- 管理端执行页和报告页展示隔离进程、Windows Job、内存上限与强停状态；父进程在最终 Job 元数据落盘后重新生成 HTML／Markdown，避免终态或报告早于隔离审计发布。
- 测试覆盖真实子进程 PID、Windows Job 绑定、工作／临时目录、无关环境密钥剥离、协作取消、卡死强杀、父进程总时限、WebSocket 私网拒绝、危险动作 IPC 确认，以及真实 API＋Chromium＋离线报告。后端完整回归 `93 passed`，前端 `16 passed`，TypeScript 和生产构建通过。
- 当前资源为 `index-CnIYErl5.js`、`index-D9qt70XW.css`；三套前端产物与一键运行后端已同步，一键运行达到 `Ready: http://127.0.0.1:8081/` 并自动退出。
- 边界说明：原 FR-11 明确要求“浏览器运行在隔离容器中”。当前机器没有 Docker，Hyper-V 计算服务未启用，本批完成的是可验证的 Windows 本地进程／Job／Chromium 沙箱隔离，不能冒充容器验收。FR-11 仍需在具备容器运行时的环境补镜像、容器文件系统和容器层默认无内网证据。
- 产品整体仍缺 FR-04／FR-09、Canvas／Cesium、企业目标站基准、30 场景重复基准及其他 Definition of Done 项，不得声明 `1.30.00` 已正式发布。

## 13. 2026-07-21 GAEALaViC 目标网站驱动需求接入

- 用户明确：GAEALaViC 指导书应定义完整目标网站测试要求，不能依据 AI-GUI `1.25.00` 或当前能力缩减；AI-GUI 必须反向根据目标测试规格更新。
- 新增独立需求文档 `C:\Users\zzzzl\Desktop\京彩OPC\GAEALaViC测试网站驱动的AI-GUI更新需求_2026-07-21.md`，原组员指导书保持不修改并继续由组员补充。
- P0 新增 placeholder 原生定位、表格行／容器作用域定位、安全上传、受控下载、异步状态机等待、测试数据生命周期、项目业务上下文包和副作用策略矩阵。
- P1 新增复杂组件适配、通用视觉手势、Cesium Adapter／Bridge、Canvas 状态断言和认证／角色矩阵；最终建立至少 30 个固定场景并重复 5 次。
- 目标系统仍需提供专用测试租户和授权、固定数据及文件样例、异步状态机、下载格式、Cesium 业务断言／Bridge 可行性和清理方案；AI-GUI 不得猜测这些事实。
- `GAE-GUI-01～14` 全部归属未来 `1.30.10`，不改变 `1.30.00` 当前优先级。FR-11 容器隔离已在第 14 节完成，当前下一开发批次恢复 FR-04／FR-09，不提前实施目标网站专用需求。
- 只有 `1.30.00` 满足企业负责人原始需求并完整验收后，才能按固定版本流程新建 `1.30.10`、完整复制 `1.30.00`、核对并冻结 `1.30.00`，之后再实施网站适配。
- 本节只同步未来需求归属，尚未实现这些新增项，不改变 `1.30.00` 未发布状态，也不影响 `1.25.00` 冻结基线。

## 14. 2026-07-21 第七批更新：FR-11 Docker 容器隔离 Runner

- Windows 11 Home 采用 Docker Desktop WSL2 Linux Engine；已启用 WSL／VirtualMachinePlatform、WSL2 默认版本和 hypervisor 自动启动。Docker Desktop `4.83.0`、Engine `29.6.2` 可用，`hello-world` 拉取成功。
- 新增 `Dockerfile.runner`、容器入口、防火墙初始化、JSON-lines Worker 和宿主 Docker Orchestrator。固定计划、逐步 Agent、同步／异步执行及稳定／自适应回放统一支持 `GUI_RUNNER_MODE=container`，Docker 启动失败时返回系统错误，不静默降级。
- 镜像固定 Python 3.12 slim 基础镜像摘要和 Playwright `1.49.1` Chromium。每次运行使用只读根文件系统、`/tmp`／Runner HOME／`/run` tmpfs，只挂载本次运行工件目录；实测 UID 10001 可以写单次工件卷，不能写根目录。
- 容器限制为 `2048 MiB` 内存、`2 CPU`、`256 PID`、`512 MiB /dev/shm`，启用 `NoNewPrivs`。入口仅在写入防火墙和降权时临时取得 `NET_ADMIN／SETUID／SETGID／SETPCAP`，启动 Worker 前 `CapEff` 与 `CapBnd` 均清零。
- 默认容器出站防火墙阻断 IPv4／IPv6 私网、回环、链路本地、CGNAT、组播和保留地址，仅按 `/etc/resolv.conf` 放行 Docker DNS 的 TCP／UDP 53；Playwright 域名白名单、子资源检查、WebSocket 和 DNS 重绑定防护继续作为第二层。显式 `allowPrivateNetwork=true` 才使用受控私网例外。
- 网络实测：默认访问同 bridge 私网服务被阻断，显式例外访问返回 HTTP 200；默认公网 DNS 正常且 Python HTTPS 返回 200。当前本机透明代理对容器 Chromium 的 `example.com` HTTPS 返回 `ERR_CONNECTION_CLOSED`、HTTP 返回 502，该外部代理现象没有被记为产品成功。
- 真实 Chromium 成功运行 `20260721-163358-cbec1fad`：授权容器目标、两个步骤均 `passed`，终态 `passed／plan_completed`。强停运行 `20260721-164841-0b5cefb5`：等待卡住后取消，终态 `cancelled_forcibly`、`forced_termination=true`，容器自动删除。
- 修复容器最终 `run.json` 写入与宿主轮询的短窗口竞态；最终文件尚未完整时回退父进程 `run-state.json`。管理端按 Docker／Windows 模式分别展示隔离信息，不再把容器误标成“Job 未绑定”。
- 自动验证：后端完整回归 `97 passed`，前端 `17 passed`，TypeScript 和生产构建通过。生产资源为 `index-CGyzRRW9.js`、`index-D9qt70XW.css`。
- 交付验证：源码、一键运行和构建成品的 HTML／JS／CSS SHA-256 一致；一键启动构建镜像后达到 `Ready: http://127.0.0.1:8081/`。从一键包自身后端启动的容器运行 `20260721-165401-c9e894f5` 为 `passed／plan_completed`，加载路径确认属于 `1.30.00\Windows一键运行\backend`。
- FR-11 在当前单用户 MVP 的测试执行范围完成，但产品整体仍缺 FR-04／FR-09、通用 Computer Use、完整 Cesium／Canvas 能力、企业基准和 Definition of Done；`1.30.00` 仍不得对外声明已发布。GAEALaViC 专站适配继续归属未来 `1.30.10`。

## 15. 2026-07-21 第八批更新：FR-04 目标优先 UX 与项目业务上下文

- 新建测试默认路径只展示目标网站地址、当前测试目标和期望结果；测试名称、执行角色和前置条件进入“高级设置”，可复用场景库折叠为可选区。高级字段仍进入规则 Planner、AI Planner 和逐步 Agent，不是无效输入。
- 测试名称允许留空，生成时使用测试目标前 80 个字符；前置条件不再是生成计划的强制字段。默认文案从手工点击流程改为顶层业务目标，避免要求用户先设计自动化步骤。
- 项目新增结构化 `businessContext`：业务范围说明、术语映射、业务对象、对象状态模型、示例目标和操作边界。旧项目缺少字段时自动加载空上下文，不破坏现有 JSON。
- 业务上下文支持创建、更新、JSON 持久化和项目修改审计；前端对术语映射及状态模型执行结构校验，后端统一去空白、去重和限制数量。
- 固定 AI 计划和逐步 Agent 均读取项目权威上下文。Prompt 将项目上下文标为用户审核的可信配置、页面内容标为不可信输入；专业术语／对象／状态／允许操作信息不足时要求阻塞，并以“需要澄清：”提出具体问题，不得猜测。
- 容器 Worker 通过内存协议继承完整 AgentScenario 业务上下文，不生成中间配置文件；单元测试直接验证容器序列化内容。
- 自动验证：后端完整回归 `98 passed`，前端 `18 passed`，TypeScript 和生产构建通过。桌面 1440×960 与移动 375×812 实测目标／期望可见、高级字段折叠且无横向溢出。生产资源为 `index-DcJZJSaS.js`、`index-poCfvuHG.css`。
- 交付同步：源码、一键运行和构建成品的 HTML／JS／CSS SHA-256 一致；一键入口重新构建 Docker 镜像并达到 `Ready: http://127.0.0.1:8082/`，最终最新交付服务在 `http://127.0.0.1:8081/` 健康返回 `ok`。
- 本批没有实现 GAEALaViC 专用术语、对象或状态，也没有修改 `1.25.00`。FR-04／FR-09 仍未完成；下一项是通用视觉动作／Computer Use fallback、动作后验证及真实自适应重新定位证据。

## 16. 2026-07-21 第九批更新：FR-04 通用视觉动作与 FR-09 真实自适应回放

- 视觉建议不再局限 Canvas 单点点击。受约束 Schema 支持 `click／hover／scroll／drag`，可相对指定区域或整个 viewport 返回起点、拖拽终点、滚动量、置信度、理由和预期变化；低置信度、动作不一致、拖拽缺终点均拒绝执行。
- 视觉模型仍只提供建议。点击、悬停、滚轮和拖拽统一由 Playwright 鼠标接口执行；每次动作记录触发原因、脱敏截图、模型、目标、动作、坐标、预期变化和 before／after。
- 动作后验证同时比较 URL、标题、DOM 摘要、Accessibility 摘要与脱敏截图字节；无可验证变化时动作失败，并写入 `visual_action_verified=false`，不能凭模型声明成功。
- 新增 `AdaptiveReplayPlanner`：普通步骤按审核计划执行，C 级视觉步骤丢弃探索期坐标，按当前截图和原语义目标重新调用视觉适配器。两次回放测试分别执行 `25%／75%` 横坐标，证明不是固定坐标重跑；stable 路径不配置 Planner 或视觉模型。
- `POST /api/runs/{run_id}/replay` 的 adaptive 模式必须显式携带本次 AI 设置；API Key 只进入内存和单次容器协议，不从历史恢复、不写入 `run.json`。本地模型可通过 Docker 官方宿主别名 `host.docker.internal` 使用 HTTP，其他远程 HTTP 地址继续拒绝。
- adaptive Planner 与视觉适配器已进入 FR-11 Docker Worker 序列化链。真实容器运行 `20260721-183446-e2727205` 为 `passed／agent_goal_completed`，隔离模式 `docker_container`，实际坐标 `region-relative:0.5000,0.5000`，动作后验证为真，API Key 持久化检查为假，结束后无残留容器。
- 自动验证：后端完整回归 `103 passed`，前端 `18 passed`，TypeScript 和生产构建通过。生产资源为 `index-DA0Eqjad.js`、`index-poCfvuHG.css`，源码、一键运行和构建成品哈希一致；镜像 `ai-gui-runner:1.30.00` 已重建。
- 范围边界：未加入 GAEALaViC 专站逻辑，`1.25.00` 未修改。FR-09 已满足 `1.30.00` 功能范围；企业环境与 30 场景重复基准转入 `1.30.10`。`1.30.00` 下一项为通用 Cesium／Canvas Adapter／Bridge、动作前稳定性，以及 FR-05／FR-06 的通用非 DOM 证据与问题分类。

## 17. 2026-07-21 版本范围修正：企业级验收转入 `1.30.10`

- 用户明确决定：企业目标网站验收不需要在 `1.30.00` 实现，后续在 `1.30.10` 实施。
- `1.30.10` 承担企业目标站专用适配、授权环境联调、至少 30 个固定场景×5 次重复运行、完成率／稳定率／证据完整率等企业级 Definition of Done。
- `1.30.00` 承担可复用通用能力、本地功能验收，以及 ER-07／ER-08 要求的公共站开发与通用性验证。因此 FR-09 的 stable／adaptive 功能链在 `1.30.00` 范围内已完成，不再因缺少企业重复基准而标为未完成。
- 历史章节中“企业目标站或 30×5 基准阻塞 `1.30.00` 发布”的表述均由本节取代。当前剩余集中在通用 Cesium／Canvas Adapter／Bridge、动作前稳定性、非 DOM 状态证据、通用语义问题分类，以及公共 Cesium 与非 Cesium 站点验证。

## 18. 2026-07-21 第十批更新：ER-05 允许操作与 Bridge 语义配置

- 项目 `businessContext` 新增 `allowedActions`、`bridgeCapabilities` 和 `bridgeSemanticTargets`；原有项目／场景 `forbiddenActions` 继续作为结构化禁止策略，允许与禁止操作不再混在自由文本中。
- `allowedActions` 非空时，固定 AI Planner 和逐步 Agent Prompt 只允许规划明确列出的业务操作；Bridge 能力与语义目标只能引用项目声明内容，不得由模型虚构。信息不足时仍必须阻塞并请求澄清。
- Bridge 能力清单描述项目允许暴露的能力，例如等待场景就绪、读取选中对象；Bridge 语义目标以稳定 ID 映射业务含义，为后续 ER-06 Adapter／Bridge 执行层提供可信输入。
- 新字段支持去空白、去重、数量限制、创建／更新、JSON 持久化、项目审计、旧项目自动补空值、固定 AI、逐步 Agent 和 Docker Worker 上下文传递。
- 前端项目业务上下文折叠区新增允许操作、Bridge 能力和 Bridge 语义目标编辑。桌面 1440×960 与移动 375×812 实测无横向溢出。
- 自动验证：后端完整回归 `103 passed`，前端 `18 passed`，TypeScript 和生产构建通过；生产资源 `index-B5x6iL-u.js`、`index-poCfvuHG.css`，三套发布目录已同步，`ai-gui-runner:1.30.00` 已重建。
- 边界：ER-05 的项目级配置范围完成；开发主线立即回到 **FR-04**。ER-06 仅作为 FR-04 的企业补充映射，不得替代 FR 编号或另行改变优先级。

## 19. `1.30.00` 未完成需求与唯一优先级（当前权威）

本节取代此前所有“下一项进入 ER-06”或把 ER 编号作为独立开发主线的表述。企业补充需求只映射到原 FR，实施顺序以 FR 主线为准。

### P0-1：FR-04 AI 探索执行（已完成）

1. 定义并实现通用 Canvas App Bridge Adapter，完整支持最小契约：`getSceneState()`、`listVisibleTargets()`、`getTargetScreenPosition()`、`getSelectedTargetId()`、`waitForSceneReady()`。
2. 实现可替换的最小 Cesium 参考适配器；Cesium 专用对象只能存在于适配层，不得泄露到通用 Agent／Executor／Compiler。
3. 补齐 `app_bridge` 动作与验证：语义目标 ID → 当前屏幕位置 → Playwright mouse 动作 → 选中目标／场景状态验证。
4. 补齐动作前稳定性：DOM 元素显式检查可见、可用、位置稳定且未被遮挡；Canvas／Cesium 必须等待场景、瓦片、实体和渲染就绪，不用固定 sleep 冒充稳定。
5. Bridge 不可用时保持 L0／L1 黑盒与视觉 fallback 可用，并明确记录降级原因、执行模式和稳定性等级。

### P0-2：FR-05 运行证据采集（已完成）

1. 每个 Bridge／Canvas 动作记录 before／after 场景快照。
2. 采集相机、时钟、图层、可见实体、选中实体、瓦片／资源加载状态，以及 Bridge 版本和能力。
3. 把语义目标 ID、坐标来源、Playwright 实际动作、场景状态变化、截图和 Trace 关联到同一步骤。
4. Bridge 缺失、超时、返回非法坐标或状态不可验证时留下结构化失败证据。

### P0-3：FR-06 问题发现与结构化报告（当前下一项）

1. 识别关键 Canvas／WebGL 对象不可见、不可交互、未高亮或选中对象错误。
2. 识别场景长期未就绪、瓦片／资源加载失败、Bridge 契约异常和动作后语义状态未变化。
3. 报告必须区分 Bridge／页面事实与 AI 推断，并保留严重度、置信度、预期／实际、复现步骤和证据引用。

### P1：`1.30.00` 发布收口

1. 完整后端、前端、TypeScript、生产构建、Docker Runner 和一键包回归。
2. 核对源码、一键运行和构建成品哈希，保留真实运行编号与失败边界证据。
3. 更新需求追踪、版本状态和接续说明后，才能判断 `1.30.00` 通用能力范围是否完成。

当前已完成且不再重复开发：FR-01、FR-02、FR-03、FR-04、FR-05、FR-07、FR-08、FR-09、FR-10、FR-11；ER-01～ER-06 的 `1.30.00` 通用能力。当前下一项为 P0-3 FR-06。ER-07 转入 `1.30.10`，ER-08 转入 `1.30.20`；ER-09 是持续流程。

### 版本归属再次确认

- `1.30.00`：只完成当前 P0-1 FR-04、P0-2 FR-05、P0-3 FR-06 及发布收口。
- `1.30.10`：完成 ER-07 Cesium 官方站／安全公共站验证及企业首个测试网站适配与验收。
- `1.30.20`：完成 ER-08 非 Cesium 业务站通用性验证及第二个正式网站适配。

## 20. 2026-07-21 第十一批更新：P0-1 FR-04 Bridge 与动作前稳定性

- 新增通用 `CanvasAppBridgeAdapter`，严格验证 Bridge v1 和五项最小契约：`getSceneState()`、`listVisibleTargets()`、`getTargetScreenPosition()`、`getSelectedTargetId()`、`waitForSceneReady()`。
- Bridge 只在所选环境明确启用时授权；支持经过校验的自定义全局名称和 `generic／cesium` 适配器。配置随固定计划、Agent、回放和 Docker Worker 传递，未启用时拒绝 `app_bridge` 动作并记录 locator／visual 降级模式。
- Bridge 点击执行链为：等待场景就绪 → 读取场景与可见目标 → 验证目标 ID 和 viewport 坐标 → Playwright mouse 点击 → 再次等待就绪 → 核对选中目标与场景状态。模型和 Bridge 均不直接操作浏览器。
- 新增显式动作前稳定性：DOM／区域动作检查可见、可用、连续两帧位置稳定且中心未被遮挡；整页视觉动作确认 viewport；Bridge／Cesium 使用 `waitForSceneReady()`，不用固定 sleep 冒充场景稳定。
- 每个步骤新增 `stabilityEvidence`，成功和失败均写结构化事件；GUI 执行详情显示动作前检查结果。Bridge 结果保留版本、能力、before／after 场景、可见目标、坐标和选中状态。
- 新增可下载的 `cesium-bridge-reference.js`：以 Cesium Viewer 安装测试 Bridge，提供相机、时钟、图层、瓦片状态、实体列表、屏幕坐标、选中状态和基于渲染帧的场景就绪等待；Cesium 对象不进入通用 Agent／Executor 接口。
- 生成的 Playwright TypeScript 对 Bridge 步骤同样执行契约、场景就绪、可见目标和选中状态验证，不再只固化一次坐标点击。
- 自动验证：后端 `110 passed`、前端 `18 passed`、TypeScript、生产构建和参考适配器语法检查通过；生产资源 `index-CNixvqn0.js`、`index-poCfvuHG.css`。
- 真实 Docker 运行 `20260721-195720-1d8a9d5e` 为 `passed／plan_completed`，隔离模式 `docker_container`，坐标来源 `app_bridge:entity.alpha`，动作前稳定性和语义状态验证均为真，选中目标为 `entity.alpha`，运行后无残留容器。
- P0-1 FR-04 在 `1.30.00` 范围完成。当前下一项为 P0-2 FR-05 Canvas／Bridge 场景证据采集；ER-07 和 ER-08 不在本版本执行。

## 21. 2026-07-21 第十二批更新：P0-2 FR-05 Canvas／Bridge 场景证据

- `StepResult` 新增 `canvasEvidence`。每个 visual／app_bridge 步骤关联执行模式、动作、语义目标、坐标来源、动作前后截图和 `trace.zip`。
- Bridge v1 新增通用状态快照采集：版本、能力、场景就绪、场景状态、可见目标和选中目标。Cesium 模式验证并保留相机、时钟、图层、瓦片／加载、实体和选中状态。
- app_bridge 动作直接关联 `sceneBefore／sceneAfter`、`selectedTargetBefore／After` 和语义验证结果；visual Canvas 动作在 Bridge 可用时同样自动采集 before／after，并标记场景或选中目标是否变化。
- Bridge 未启用、契约缺失、场景超时、坐标非法、执行失败或动作后状态不可读时，记录 `collectionStatus=failed`、失败阶段、错误、截图和 Trace，不把证据缺失误写成成功。
- 固定计划和逐步 Agent 使用同一证据实现。Agent 的 Canvas 证据额外记录 Observation 是否证明有进展；无 Bridge 的 L0／L1 视觉步骤仍保留截图、坐标与页面 Observation。
- GUI 执行详情可展开 Canvas／Bridge 证据；下载的 Markdown／HTML 报告嵌入完整结构化证据，JSON 和事件时间线继续保留原始脱敏字段。
- 自动验证：后端 `111 passed`、前端 `18 passed`、TypeScript 和生产构建通过。生产资源 `index-DhkCM0B8.js`、`index-poCfvuHG.css`，三套发布目录已同步并重建 Docker 镜像。
- 真实 Docker 运行 `20260721-221430-51794b69` 为 `passed／plan_completed`，`canvasEvidence=complete`，before／after 场景、截图和 Trace 均存在，语义状态与选中目标验证通过，运行后无残留容器。
- P0-2 FR-05 在 `1.30.00` 范围完成。当前下一项为 P0-3 FR-06 Canvas／Bridge 问题发现与结构化报告。

## 22. 2026-07-21 第十三批更新：P0-3 FR-06 与 `1.30.00` 发布收口

- 新增七类确定性 Canvas／Bridge Finding：`canvas_target_not_visible`、`canvas_target_not_interactive`、`canvas_selection_mismatch`、`canvas_state_unchanged`、`canvas_scene_not_ready`、`canvas_resource_failure`、`bridge_contract_error`。
- 分类直接读取 `collectionStatus`、失败阶段、Bridge 错误、语义目标、可见目标、动作前后场景、选中对象、加载／瓦片状态及语义验证结果。Bridge／Canvas 直接状态统一进入 `facts`；不确定根因只进入 `inference`，不把推测冒充事实。
- 专用 Finding 保留严重度、置信度、预期／实际、复现步骤、动作前后截图和证据时间线。同一步已经有专用 Canvas Finding 时抑制无信息量的通用“步骤失败”，运行时异常和页面问题等独立证据仍保留。
- 测试覆盖 Bridge 契约缺失、目标不可见、目标坐标不可交互、选中对象错误、场景仍在 loading／`tilesLoaded=false`、资源失败、语义状态不变和正常 Bridge 零误报。Generic／Cesium 真浏览器定向回归为 `16 passed`。
- 完整验证：后端 `118 passed`、前端 `18 passed`、TypeScript 无错误、生产构建成功；生产资源保持 `index-DhkCM0B8.js`、`index-poCfvuHG.css`。
- 真实 Docker 验收 `fr06-container-20260721-224815` 在公共页面执行缺失 Bridge 契约的动作，唯一 Finding 为 `bridge_contract_error`；JSON、Markdown、HTML 均包含直接事实、截图和时间线。首次未传递相同 `run_id` 的探针被只读父目录正确拒绝，没有绕过容器文件系统边界。
- 一键包从自身后端重建 `ai-gui-runner:1.30.00`，启动达到 `Ready: http://127.0.0.1:8086/`；包内真实容器运行 `fr06-package-20260721-225913` 同样只生成 `bridge_contract_error`。运行后无 `ai-gui-*` 残留容器。
- `源码\dist`、`Windows一键运行\dist`、`构建成品` 的入口、JS、CSS、服务端 bundle SHA-256 一致；FR-06 后端与测试已同步到一键运行版且哈希一致。最新源码服务在 `http://127.0.0.1:8083/` 健康返回 `ok`。
- P0-3 FR-06 和 P1 发布收口已完成。FR-01～FR-11 现已完成 `1.30.00` 约定的通用单用户功能范围，可以进入冻结和建立下一版本的流程；该结论不包含企业目标站适配或企业级验收。
- 后续版本边界保持不变：`1.30.10` 承担 ER-07、GAEALaViC／首个企业测试网站适配与企业验收；`1.30.20` 承担 ER-08 和第二个非 Cesium 业务网站通用性验证。`1.25.00` 未修改。

## 23. 2026-07-21 `1.30.00` 正式冻结

- 用户确认冻结 `1.30.00` 并建立 `1.30.10`。本版本最终基线为 FR-01～FR-11 通用单用户功能范围完成，后端 `118 passed`、前端 `18 passed`、TypeScript 与生产构建通过。
- `1.30.00` 自本节起只读保存，不再接收功能、测试、构建或文档更新；后续开发只在完整复制出的 `1.30.10` 中进行。
- `1.30.10` 负责 ER-07、GAEALaViC／首个企业测试网站适配、授权数据与角色矩阵及企业验收；`1.30.20` 继续负责 ER-08 和第二个非 Cesium 业务网站验证。

## 24. 2026-07-21 `1.30.10` 初始化验证

- `1.30.00` 完整复制结果为 30,800 个文件、606,275,155 字节；唯一由普通 Windows 复制遗漏的深层 pytest 临时零字节 `.network` 文件已通过长路径 API补齐，最终文件数和总字节一致。
- 新目录和状态文档已改名为 `AI-GUI 1.30.10`、`1.30.10版本更新与需求状态.md`；源码和一键版默认 Docker 镜像、两个启动器及对应测试统一使用 `ai-gui-runner:1.30.10`。
- 初始化回归：后端 `118 passed`、前端 `18 passed`、TypeScript 与生产构建通过；三套前端产物的 HTML、JS、CSS 和服务端 bundle SHA-256 一致。
- Docker 镜像 `ai-gui-runner:1.30.10` 构建成功。一键包检测并纠正复制虚拟环境的旧 editable 安装，确认模块重新安装自 `1.30.10\AI-GUI 1.30.10\Windows一键运行\backend`，最终达到 `Ready: http://127.0.0.1:8086/` 并自动退出。
- `1.30.10` 初始化完成；下一项应先读取完整的 GAEALaViC 测试指导和既有目标站需求清单，建立可验收的缺口矩阵，再按 P0 顺序实施，不能回写冻结版本。

## 25. 2026-07-21 GAEALaViC 最终指导书转化为 `1.30.10` 需求

- 已完整读取 `AI-GUI项目下一阶段开发指导书-GAEALaViC接入闭环(1).md`。该指导书是独立目标标准，不依据 GUI 当前能力缩减，现作为 `1.30.10` 首站适配和企业验收的上游依据。
- 新增权威需求基线 `C:\Users\zzzzl\Desktop\京彩OPC\AI-GUI_1.30.10_GAEALaViC更新需求基线_2026-07-21.md`。原 `GAE-GUI-01～14` 保留并细化，新增 `GAE-GUI-15` 完整页面地图／低风险探测、`GAE-GUI-16` 标准证据包／完整率和 `GAE-GUI-17` L4 跨模块业务闭环。
- 继承而不重复开发：项目环境、网络与密钥、动态 Agent、Docker 隔离、通用视觉、Bridge v1、Canvas 证据／Finding、代码生成与回放；这些能力仍需在目标站重新验收。
- 当前产品缺口：placeholder／稳定属性和容器作用域定位、业务上传下载、WebSocket 事件与异步状态机、业务对象依赖／清理、多账号角色、副作用矩阵、完整页面地图、GAEALaViC 业务包和组件适配、目标站 Cesium 语义、标准证据包、30×5 基准和 L4 闭环。
- 当前下一项固定为 P0-1：`GAE-GUI-15／01／02／13`，先完成页面地图、登录／角色、placeholder 和业务对象作用域定位；不得先用裸坐标硬适配。
- 目标系统依赖包括授权 URL／私网、测试租户与角色矩阵、固定模板和数据文件、状态／WebSocket 协议、上传下载格式、Cesium 语义／Bridge 可行性、清理授权和企业脱敏边界。缺失时对应场景必须 blocked，不得猜测。
- 发布门槛：S01～S30 均有可执行场景、每场景 5 次、完成率 >=80%、stable >=95%、证据 >=95%、清理 100%，且 L4 至少完整成功一次；高风险误操作、明文泄露、裸坐标稳定用例和未授权域名访问均为 0。

## 26. 2026-07-22 P0-1 页面地图、稳定作用域定位与多账号会话

- `GAE-GUI-15`：兼容性扫描新增 `read_only／low_risk` 模式和持久 `app-map.json`，页面地图覆盖路由、主区域、菜单、页签、按钮、输入、下拉、上传、表格列、卡片字段、弹窗、Canvas、iframe、Shadow DOM、无名称控件和高风险动作；新增 `GET /api/projects/{project_id}/app-map`。低风险探测仅允许页签、导航和打开受控弹窗，浏览器路由层阻断所有非 GET／HEAD／OPTIONS 请求。
- `GAE-GUI-01`：Locator、Observation、Planner、人工计划编辑器和 Compiler 原生支持 `placeholder`、`data-test`、`data-qa`、`data-cy`、`data-object-id`。动作定位必须唯一，多匹配明确失败，不再静默使用第一个元素。
- `GAE-GUI-02`：新增 `row／card／dialog／tab_panel／canvas` 作用域，作用域可使用稳定属性并绑定业务对象身份；Compiler 生成嵌套 Playwright locator 和 `hasText` 身份过滤。真实重复按钮测试证明只命中 `data-object-id=agent-2／E2E_Beta`，未限定作用域时以“实际匹配 2 个”拒绝执行。
- `GAE-GUI-13`：项目新增账号角色矩阵；每个账号具有独立 DPAPI 会话文件和 `projectId:accountId` 熵，项目文件与会话元数据只保存凭据别名、不保存明文。扫描、固定运行、Agent、回放、会话导入／录制和报告均显式携带账号 ID／角色；删除一个账号会话不影响其他账号。
- 前端新增账号矩阵 JSON、当前账号选择、只读扫描／低风险探测分离按钮，以及 placeholder、稳定属性、作用域和业务身份编辑。新增回归覆盖账户持久化与低风险请求、稳定属性作用域计划审核。
- 自动验证：P0-1 后端定向 `41 passed`，后端完整 `123 passed`，前端 `20 passed`，TypeScript、生产构建通过。三套前端产物 5 个文件逐项 SHA-256 一致；17 个生产后端文件在源码与一键包逐项哈希一致。
- 最终 Docker 镜像为 `sha256:329a4a06a2ed9464c2319b760fc1cbd45473ef405c07369dd87cac87aa6eb26d`。官方 PyPI 经当前 Docker 代理持续 SSL 中断，Dockerfile 已增加可覆盖的镜像源与 120 秒超时；本机最终镜像从已验收依赖基底覆盖当前 `/app/src`，并确认运行时从该路径加载新模型。
- 真实容器验收 `P01ACCEPT` 为 `passed／plan_completed`：placeholder 填写、重复按钮的 row＋稳定属性＋业务身份定位、动作前稳定性和断言全部通过，结果记录 `account_id=admin／account_role=administrator`。一键包达到 `Ready: http://127.0.0.1:8087/` 后自动停止；无运行中容器或监听残留。
- 状态边界：P0-1 的 GUI 通用能力实现与本地容器验收完成；GAEALaViC 目标站的五个主模块页面地图及 S01～S06、S11、S30 尚需授权 URL、账号和权限矩阵后验收，不能标记为目标站通过。
- 当前下一开发项固定为 P0-2 `GAE-GUI-03／04`：项目测试文件仓库、受控上传／下载、manifest、SHA-256 和内容结构证据。不得提前跳到 P0-3、Cesium 专站适配或 `1.30.20` 的 ER-08。

## 27. 2026-07-22 P0-2 测试文件仓库与受控上传／下载

- `GAE-GUI-03` 新增项目级 `test-data-manifest.json` 和测试文件仓库。API 以原始请求体流式写入，单文件上限 1 GiB，不使用 JSON／base64；记录文件 ID、名称、大小、SHA-256、MIME、扩展名、校验 profile、有效性、错误、预期处理结果和时间，不在 manifest 中保存正文或宿主路径。
- 支持 JSON、GeoJSON、ZIP／KMZ、1201／3601 HGT、PNG／JPEG／TIFF、KML／Shapefile 组件、CSV 和二进制 profile。未知扩展名、错误 JSON／GeoJSON、损坏 ZIP、错误 HGT 大小、错误图片签名等作为 `invalid` 负向样例登记，不冒充合法输入。
- 新增 `upload` 动作：只能引用当前项目 manifest 中的 `file_id`，只允许在明确选定的项目测试环境执行，业务对象名必须使用 `E2E_` 前缀。计划必须声明预期有效／无效；无效样例必须提供残留对象 locator 并验证期望数量，默认零残留。
- Docker Runner 启动前只复制计划引用的登记文件到本次运行挂载 `_runner_inputs`，容器计划只看到运行内路径；结束、取消和强停均由编排器删除暂存目录。文件正文、宿主路径和上传 body 不进入模型上下文、报告或事件。
- 上传证据包含 manifest 元数据、业务对象名、请求方法／脱敏 URL／资源类型、响应状态和白名单响应头、响应中提取的业务 ID、网络收敛、残留对象检查以及 `contentExposedToModel=false`。不记录请求 body、认证头、Cookie 或文件正文。
- `GAE-GUI-04` 新增 `download` 动作。只有计划包含下载时固定 Runner 才启用 Playwright download；文件名经过路径规范化并限制在 `run/downloads`，同名文件自动编号，不能写任意路径。Agent 模式仍必须显式返回受约束 download 动作。
- 下载证据通过 Chromium CDP `Fetch` 响应阶段只读捕获状态和白名单响应头，立即继续响应，不复制响应体或发起第二次 GET；保存后记录文件名、URL（去查询参数）、大小、SHA-256、格式和结构校验。支持 JSON 必需键、ZIP CRC、CSV 表头、UTF-8 文本、扩展名、文件名模式、最小大小和可选预期哈希。
- Playwright TypeScript 生成器使用 `TEST_FILE_<FILE_ID>` 环境引用上传文件，下载写入 `testInfo.outputPath`，同步生成大小、SHA-256、扩展名、JSON 键或 ZIP 签名检查；不固化宿主路径。固定运行、Agent、stable／adaptive 回放和 Docker Worker 均传递相同文件能力，回放会重新核对当前 manifest。
- 管理端项目页新增文件登记、profile、预期结果、有效性、错误、大小、哈希和删除；计划编辑器与路径审核支持 upload／download、登记文件、预期有效性、E2E 对象、残留检查和下载验证；运行详情、JSON、Markdown、HTML 展示文件证据与下载工件。
- 自动验证：P0-2 定向 `42 passed`，后端完整 `128 passed`，前端 `21 passed`，TypeScript 和生产构建通过。56 个后端生产文件在源码／一键包零差异；三份前端产物 5 个文件逐项 SHA-256 一致。
- 最终 Runner 镜像为 `sha256:a7a8040df0b130749038f7a53ea754a06236c1c7b6d3421553d69eb63373345c`。真实 Docker 运行 `20260722-022720-6367867c` 为 `passed／docker_container`：上传提取业务 ID `E2E_Agent_Docker_10`，下载响应 `200／application/json`，结构与 SHA-256 通过，下载工件存在，输入暂存已清除。运行后无残留容器。
- 一键包从当前后端达到 `Ready: http://127.0.0.1:8083/` 后自动停止。冻结版遗留的 `8083／8084` 服务已停止，避免继续写入 `1.30.00` 日志；冻结版源码、测试和产物未修改。
- 状态边界：P0-2 的 GUI 通用能力与本地／Docker 验收完成；GAEALaViC 的合法／非法 JSON、ZIP、HGT、GIS、图片样例，真实上传 schema、对象查询方式及仿真／评估下载格式尚未由企业提供，因此 S07／S08、S17～S24、S28 目标站验收继续 blocked，不能标记为通过。
- 当前下一开发项固定为 P0-3 `GAE-GUI-05／06／08`：异步状态机与 WebSocket 证据、业务对象依赖／逆序清理、对象级副作用策略矩阵。不得提前进入 P1 Cesium 专站适配或 `1.30.20` ER-08。

## 28. 2026-07-22 P0-3 异步状态、业务清理与副作用矩阵

- `GAE-GUI-05` 已实现项目级异步状态机、`wait_for_state` 动作、允许／终态／失败态／迁移校验、显式轮询间隔和总超时。成功、失败、未知状态、非法迁移和超时均输出业务对象关联的状态时间线；生成的 Playwright 测试通过 `ASYNC_STATE_MACHINE_<ID>` 环境配置复现同一规则。
- WebSocket 证据采集已接入固定计划和 Agent：记录连接、帧方向、关闭、错误及按 URL 计算的重连次数；帧只提取业务 ID、状态和事件白名单字段，不保存 token、Cookie、认证头或完整 payload。
- `GAE-GUI-06` 已实现场景级业务对象依赖图，校验缺失依赖、环、`E2E_` 前缀和受约束 cleanupStep。主流程成功或失败后均在浏览器关闭前逆拓扑清理；逐对象继续收集结果并验证不可查，输出 `cleanup-report.json`、残留、失败和人工处置项。
- `GAE-GUI-08` 已实现项目级对象副作用策略矩阵，匹配动作分类、对象类型、名称规则、环境、角色和前置状态，支持 allow／confirm／conditional／forbid。非 `E2E_` 副作用保持绝对禁止；无匹配规则保守进入现有一次性确认，项目／场景禁止动作不被弱化。
- 管理端新增异步状态机、副作用矩阵和业务对象生命周期 JSON 编辑器；运行 JSON、Markdown、HTML 及 GUI 数据映射包含 `asyncEvidence`、`sideEffectEvidence`、WebSocket 时间线和清理报告。
- 自动验证：后端完整 `137 passed, 1 skipped`，跳过项为默认不自动运行的 Docker 发布验收；该项已显式启用并另行 `1 passed`。前端 `21 passed`，TypeScript 和生产构建通过，资源为 `index-BxWLwy_b.js／index-ePCDFdM0.css`。
- 最终 Docker 镜像为 `sha256:ffe89e1b9d0ff2b573e69e78bdea64a8760225e0ce2b7124fc253a9804cc2197`。容器运行生成成功终态、3 条 WebSocket 时间线事件和通过的清理报告，运行后无残留容器。一键包已在 `http://127.0.0.1:8083/` 健康运行；66 个生产后端文件在源码／一键包哈希一致。
- P0-3 只标记 GUI 通用能力与本地／Docker 验收完成。企业目标站状态协议、WebSocket 事件、对象依赖、查询／清理接口和副作用授权矩阵尚未提供，因此 S09／S10／S12～S16／S25～S27／S29 及相关跨模块验收继续为 `blocked`，不能冒充通过。
- 当前下一开发项前移到 P1-1 `GAE-GUI-07／09`：把已确认的目标站业务上下文注入规划／执行并覆盖九个模块复杂组件。P1-2 Cesium 专站语义和 `1.30.20` ER-08 不得提前混入。

## 29. 2026-07-22 P1-1 业务上下文与九模块复杂组件

- `GAE-GUI-07` 在既有上下文包上新增有来源的业务事实、对象关系、缺失事实和来源修订。`confirmed` 事实／关系必须填写来源；未确认内容只能为 `blocked`。新增上下文状态 API，输出确认数、总数、完整度和逐项阻塞原因。
- 固定 AI 计划、逐步 Agent、Runner、Docker Worker 和最终报告都携带同一业务上下文快照。Planner 禁止使用 `blockedItems`，上下文不足时必须返回需要澄清；本地规则规划把阻塞项作为警告返回，不猜测专业对象、状态或权限。
- `GAE-GUI-09` 新增受约束 `component` 语义动作，覆盖级联下拉、可搜索选择器、日期时间范围、分页、统计卡片、标签页、上传弹窗、图片预览和局部滚动。每类动作校验必需定位器、参数和可验证结果，并输出 `componentEvidence`。
- 项目级 `componentAdapters` 按登录、全局导航、装备智能体、环境资产、高程数据、想定、运行、强化学习、帮助九模块组织。关联项目运行必须引用 `configured` 适配条目且动作与审核配置完全一致；`blocked` 条目或临时选择器拒绝执行，通用 Runner 不含 GAEALaViC 选择器。
- 新增 `backend/examples/gaealavic-project-adapter.template.json`。模板列出九模块但全部保持 blocked，并明确所缺企业页面结构／接口事实，没有把示例文字冒充真实适配。
- 管理端新增事实、对象关系、缺失项、来源修订和九模块适配包编辑，显示上下文完整度／阻塞项；计划编辑器支持复杂组件语义配置。运行 JSON、Markdown、HTML 和前端映射展示上下文快照及组件证据。
- 修复 P0-3 配置持久化缺口：项目 `asyncStateMachines／sideEffectPolicies`、场景 `businessObjects` 以及本批 `componentAdapters` 现均由 API 创建／更新模型显式接收、验证、审计和保存，不再被请求模型忽略。
- 自动验证：P1-1 定向 `42 passed`，后端完整 `145 passed, 2 skipped`；两个 skip 是需显式启用的 P0-3／P1-1 Docker 发布验收，P1-1 已另行 `1 passed`。前端 `21 passed`，TypeScript 和生产构建通过，资源为 `index-ByC0iRYo.js／index-ePCDFdM0.css`。
- Docker 运行 `20260722-033227-d6bf997a` 为 `passed／docker_container`，三个已配置组件动作及 blocked 上下文快照在容器内完整传递；最终镜像 `sha256:15215d442dd34f667c28b7eece23819e463e12ab31515cae6e709bec6619fff7`，无残留容器。68 个生产后端文件源码／一键包哈希一致。
- P1-1 通用能力完成不等于 GAEALaViC 九模块适配完成。授权 URL、页面结构、稳定属性、权威术语／对象关系／状态和企业确认来源未提供，目标站适配模板继续 blocked。
- 当前下一项为 P1-2 `GAE-GUI-10／11／12`：Canvas 手势补齐、可替换 GAEALaViC Cesium Adapter 和业务断言／Finding；ER-07 公共 Cesium 站验证仍在 `1.30.10`，ER-08 非 Cesium 第二站仍属于 `1.30.20`。

## 30. 2026-07-22 P1-2 Canvas 手势、GAEALaViC Adapter 与语义断言

- `GAE-GUI-10` 新增 `visual_zoom／visual_clear／visual_draw_polygon／visual_draw_rectangle`。缩放记录中心与滚轮量；多边形强制至少 3 点；矩形强制 2 个边界点；清除使用受约束控件。所有几何点保存为 Canvas 内相对坐标，运行时结合实时边界投影，证据含 Canvas 边界、viewport、相对／绝对点、结束方式和坐标策略，稳定性只能 B／C。
- 动作前继续验证 Canvas 区域可见、稳定和未遮挡；无 Bridge 时如实保留视觉证据，不提升为 A。固定 Runner、Agent、stable／adaptive 回放、Docker Worker 和 Playwright 生成器使用同一动作模型。
- `GAE-GUI-11` 新增独立 `gaealavic_cesium_adapter.js` 和环境 Adapter 类型 `gaealavic_cesium`。适配包在内部接收 Cesium Viewer 与企业 hooks，对外仍只暴露 Bridge v1；归一化状态覆盖图层 ID／名称／可见性、相机经纬高／姿态、实体数、选中实体、路径点、POI、围栏、绘制对象、tilesLoaded 和 WebGL 错误。缺少任一企业 hook 或语义字段即拒绝，不提供猜测默认值。
- `GAE-GUI-12` 新增十类 Canvas 业务语义断言：图层可见、相机状态／容差、实体数、选中实体、路径点数、POI 数、围栏数、绘制对象数、瓦片完成、无 WebGL 错误。断言直接读取归一化 Bridge 状态，Runner／Compiler／GUI／JSON／Markdown／HTML 均保留语义证据。
- 语义断言失败新增专用 Finding：layer／camera／entity／selection／path／POI／fence／drawing mismatch、tiles not loaded、WebGL error。Finding 写入直接场景事实和 `test_data_or_product／permission／environment／timing` 等失败域，明确不能仅凭断言把根因定为产品缺陷。
- 管理端计划编辑器新增四种 Canvas 手势控件、相对点、Canvas 区域、缩放量、清除控件和结束方式；断言编辑器新增十类语义断言与相机容差；环境页可下载 GAEALaViC Adapter，报告页展开语义状态。
- 自动验证：P1-2 定向 `66 passed`；后端完整 `161 passed, 3 skipped`，三个 skip 为显式 Docker 发布验收，P1-2 已另行 `1 passed`；前端 `21 passed`，TypeScript、生产构建和 Adapter JS 语法检查通过。资源为 `index-DpjpL4B6.js／index-ePCDFdM0.css`。
- Docker 运行 `20260722-035626-801c6fe2` 为 `passed／docker_container`：三点多边形相对手势及图层、路径点、瓦片、WebGL 四项 GAEALaViC 语义断言通过。最终镜像 `sha256:b7b9670d100d4e8a34285196ac389e73ee790529ed4d691bb9a5cc245e5f726b`，无残留容器；69 个生产后端文件源码／一键包零差异。
- GAEALaViC 企业 Viewer hooks、图层／对象 ID 规则、路径／POI／围栏／绘制数据结构仍未提供，因此企业目标站 Adapter 配置与对应场景继续 blocked。ER-07 Cesium 官方／安全公共站验证尚未执行，不能写成通过。
- 当前下一项为 P1-3 `GAE-GUI-16` 标准证据包与完整率；之后进入 P2 `GAE-GUI-14／17` 和 ER-07／企业目标站最终验收。ER-08 仍属于 `1.30.20`。

## 31. 2026-07-22 P1-3 标准证据包与完整率完成

- 每次固定／Agent 运行生成 `evidence/` 标准目录，包含运行元数据、脱敏项目／环境／业务上下文、app-map、动作与浏览器观察、WebSocket、模型调用、Finding、确认和清理；清单逐项标记 `present／missing／not_applicable` 及原因。
- 所有清单条目关联同一 run ID，并携带适用 step ID 和业务 ID。环境快照只保存普通变量名和密钥别名，不保存变量值、密钥目标或密钥值。
- 自动计算适用证据完整率，已接入 GUI、`run.json`、Markdown 和 HTML；缺失项与清单下载链接可见。Docker P1-3 验收 `1 passed`，证据完整率 >=95%，容器内清单结构与 run ID 关联通过。

## 32. 2026-07-22 P2 基准能力与 ER-07 公共站验证

- `backend/benchmarks/gaealavic/scenarios/S01.json`～`S30.json` 已逐文件固化环境、角色、前置数据、业务步骤、断言、危险策略、清理和证据要求。缺失企业事实逐项写入 `blockedDependencies`，不猜测路由、选择器、账号、状态或文件结构。
- 新增固定 30×5 调度与统计器，blocked 仍产生 150 个尝试并计入分母；统计完成率、stable 成功率、adaptive／视觉介入、加权证据完整率、清理率、失败分布和四类零容忍指标。输出 `acceptance-summary.json` 与 `acceptance-report.md`，失败回链 run／step／业务 ID／证据。
- L4 契约独立固化登录、模型、环境／高程、想定、实例、启动、状态等待、记录、双下载、逆序清理和证据阶段；L4 未完整成功时独立门禁失败，不能被总体完成率掩盖。当前真实验收报告为 `blocked`、150/150 尝试、30 个场景 blocked。
- ER-07 使用真实公共站完成：Cesium Sandcastle 页面显示 Cesium 1.143.0，iframe 内确认 `.cesium-viewer` 与 Canvas；AI-GUI 受限域名运行完成 app-map、异步 iframe 等待、Gallery／URL 断言和标准证据。`example.com` 安全公共站同样通过。验证中修复 `wait_for` 在元素异步挂载前被唯一性门禁提前拒绝的问题。
- 发布回归：后端 `168 passed, 6 skipped`，其中 P1-3 Docker 和 ER-07 两项公共站均已另行显式通过；前端 `21 passed`，TypeScript 与生产构建通过。最终资源 `index-D_CAwneV.js／index-ePCDFdM0.css`；110 个生产后端文件和 5 个 dist 文件在源码／一键包／构建成品零差异。
- 最终 Runner 镜像 `sha256:93d25f7ae19126215220f797f8a95fd26c4ab99ca368efc460f9dd0847f60f24`，无残留容器。同步后服务在 `http://127.0.0.1:8083/` 健康运行，企业验收页返回 30 场景、150 计划运行和 30 blocked。
- `1.30.10` 尚未封装。企业授权 URL、账号权限矩阵、固定样例、九模块映射、状态／WebSocket 协议、对象查询／清理接口、下载结构、Viewer hooks、删除／运行授权和 150 次时间窗口未提供；因此 30×5 真实目标站运行、L4 至少一次完整成功及最终发布门槛仍未完成。ER-08 仍属于 `1.30.20`。

## 33. 2026-07-22 企业目标地址复核与 L0 只读探测

- 重新审计现有指导书后确认此前“缺授权 URL”的表述不准确：已有目标地址 `http://192.168.31.218:7991` 和测试账号 `test001`；密码必须使用运行时密钥，租户号仍未提供。
- 已在 1.30.10 数据目录创建 `GAEALaViC 企业验收` 项目，启用显式私网例外，allowed host 限定 `192.168.31.218`。账号槽位只保存 `TENANT_CODE／LOGIN_USERNAME／INTRANET_TEST001_PASSWORD` 引用，不保存真实值。
- 执行只读兼容性扫描时，Playwright 在访问首页阶段返回 `net::ERR_CONNECTION_TIMED_OUT`，未到达登录页，也未执行点击、填写或任何副作用动作。当前阻塞归类为验收网络／环境不可达，不归类为产品缺陷。
- S01／S02 与 L4 契约已把“缺授权 URL”改为“目标地址连通性与验收网络窗口”；blocked 验收报告已重新生成并同步一键包。版本继续未封装。
- 后续复测显示 TCP 7991 已可建立连接，但 HTTP 请求返回 `Empty reply from server`，HTTPS 握手超时；目标端口当前没有提供可用的 Web 页面响应，仍无法进入登录页。恢复应用层 HTTP 服务后才能继续。

## 34. 2026-07-22 指导书功能闭环、30×5 产品化与 L4 编排完成

- 用户明确调整完成口径：公司内网真实运行不作为 `1.30.10` 功能封装前置条件。本版本以两份组员指导书为独立标准实现 GUI；真实企业结论继续标记 `unverified`，不得冒充通过。
- GAEALaViC Adapter 模板已写入指导书确认事实：五个主路由、账号菜单中的标准／开发者模式、图层／卫星图层、运行状态和版本；装备智能体批量／导出／修改／预览及 17 个详情页签；环境资产上传／GIS／缩略图；HGT 1201／3601 与 Cesium 区域预览；运行 DoE／时间／批量删除／导出；强化学习启动／停止／结果／删除；三类帮助手册、局部滚动和图片。未确认的无文本图标、隐藏 input、训练状态机与企业 hook 保持 blocked，不编造 selector。
- 新增场景运行时编译／绑定器。S01～S30 的抽象步骤和断言可绑定为严格 `TestPlan`，并在启动时校验项目、环境、账号角色、密钥引用、登记文件和复杂组件 Adapter；缺失项返回精确阻塞依赖，不再永久停留在不可执行 JSON。
- 新增持久 30×5 批次：固定 150 次，支持启动、状态／进度、取消、恢复、失败重试、JSON／Markdown 下载。`dryRun` 可在无内网时验证调度合同，但强制输出 `releaseStatus=unverified／verificationStatus=dry_run_only`。
- 新增 L4 编排器：校验依赖顺序和必需输出，在阶段间传递业务 ID／哈希／运行 ID，记录状态时间线；失败时按完成阶段逆序清理，缺少或失败的清理写入人工处置清单。支持 dry-run 和真实阶段计划／输出路径绑定，dry-run 不冒充目标站执行。
- 企业验收 GUI 已加入 30×5 进度、取消／恢复／重试、报告下载、S01～S30 绑定 JSON，以及 L4 dry-run／真实执行、结果与人工清理显示。
- 完整验证：后端 `179 passed, 6 skipped`；前端 `21 passed`；TypeScript 与生产构建通过；显式 Docker 发布验收 `4 passed`。新镜像 `sha256:e8bf519c1bef6e59baa763387a52a479652f42846f44dcc278e200095507fa11`，无残留容器。
- 源码、一键运行和构建成品 5 个前端文件哈希一致，资源为 `index-CIO-IXCF.js／index-CViZXObd.css`；180 个后端源码／benchmark／example 文件在源码与一键包零差异。
- 一键包 `http://127.0.0.1:8084/` 已实际运行。批次 `acceptance-20260722-113030-4ddc0ff2` 完成 150/150 dry-run；L4 `l4-20260722-113033-172d0b24` 完成 11 阶段合同演练，状态为 `unverified／dry_run_only`。
- 功能状态：`1.30.10` 已达到“依据指导书功能完整、可封装”口径。企业目标站已验证状态仍待公司网络、真实账号／数据／hook 下执行 30×5 和至少一次真实 L4；ER-08 继续属于 `1.30.20`。
- 已生成精简交付压缩包 `AI-GUI 1.30.10.zip`，包含源码、一键运行、构建成品和封装说明，不包含 node_modules、虚拟环境、pytest 缓存、历史 artifacts／verification。压缩包共 563 项、3,158,113 字节，SHA-256 为 `DC5AB5B6B0D5353043701C1B31A406256DB5DED6FACBC49FE3F74A24DEFB3A6C`；相邻 `.sha256` 文件可直接核验。

## 35. 2026-07-22 Windows 一键启动器热修复

- 用户报告 `1.30.00`、`1.30.10` 的 `start.bat` 无法启动。诊断确认两版应用包、Python 虚拟环境和前端产物完整；共同直接原因是 Docker Desktop 已安装但 Linux Engine 未运行，而 FR-11 要求容器隔离，不能回退宿主进程。
- `1.30.00` 继续保持冻结，未修改任何文件；使用前必须先在 Docker Desktop 中确认 Linux Engine 为 Running。
- `1.30.10` 仅修复交付启动器：已有 Chromium 时跳过重复安装；Docker 未就绪时自动启动 Docker Desktop并等待最多 3 分钟；Docker 普通 stderr 警告不再被 PowerShell 误判成致命异常；失败窗口保留退出码和可操作提示。
- PowerShell 语法、Chromium 1148／headless shell／FFmpeg 本地完整性和批处理错误路径已验证。受当前托管命令权限限制，无法代替用户启动需要提升权限的 `com.docker.service`；最终 Linux Engine 启动需在 Docker Desktop 界面完成。
- 热修复已回填交付压缩包。新包大小 `3,156,001` 字节，SHA-256 `8FF6F704FDF43A6C56E0A47D4AA05411982F2FDE28E8F6FC599A7F4837F2B980`；相邻 `.sha256` 已同步，替代第 34 节记录的原始封装哈希。

## 36. 2026-07-22 冻结版本便携交付兼容性补丁

- `1.30.10` 业务功能继续保持冻结。本次修改仅处理 Windows 交付兼容性、交付文档和重新封装，不改变 GAEALaViC 功能范围、S01～S30／L4 合同或企业目标站 `unverified` 状态。
- `Windows一键运行` 改为完整便携运行：固定使用包内 Python `3.12.10`、固定依赖，以及 Playwright `1.49.1` 对应的包内 Chromium／headless shell／FFmpeg；不再依赖系统 Python、`py` 或 `%LOCALAPPDATA%` 浏览器缓存。
- Runner 镜像以 `runtime/images/ai-gui-runner-1.30.10.tar`（或同名 `.tar.gz`）离线随包交付。本机不存在镜像时只执行 `docker load`，并在加载后验证固定 tag；启动器已禁止在线 `pip install`、`playwright install` 和 `docker build`。
- Docker Desktop 自动启动与最多 3 分钟 Engine 等待继续保留；Docker／WSL／虚拟化的首次管理员预置仍是机器级前提，容器不可用时不降级到宿主进程。
- 当前 `Windows一键运行/start.bat` 已成功启动到 `http://127.0.0.1:8081/` 并通过健康检查。删除本机 Runner 镜像后，启动器从随包 tar 成功恢复；镜像 ID 为 `sha256:e8bf519c1bef6e59baa763387a52a479652f42846f44dcc278e200095507fa11`。
- 验证结束后已停止服务并清理退出旧的 `ai-gui-*` 容器，残留容器为 0。
- 第 34／35 节的 ZIP 大小和哈希是便携大文件加入前的历史封装记录，已被本次交付补丁取代。最终 ZIP 已从干净暂存目录生成并再次解压验收：不含 `.venv`／`node_modules`，在目标镜像不存在且代理指向不可用地址时，仅运行 `start.bat` 即从包内 TAR 恢复固定镜像并在 `http://127.0.0.1:8081/` 健康启动。
- 最终 `AI-GUI 1.30.10.zip` 共 `3,062` 项、`1,087,029,573` 字节，SHA-256 `53622A5B953CDEBA268135895BA9D7660E35ED262AE6BD158C9128DB8A4886D6`；相邻 `.sha256` 文件一致。企业目标站真实 30×5／L4 状态仍为 `unverified`。
- 端口生命周期补丁使用 Windows Job Object `KILL_ON_JOB_CLOSE` 绑定 Uvicorn；直接关闭启动窗口时由系统回收服务进程树。`server.pid` 与 `stop.bat` 采用包路径、便携 Python路径、启动时间和 Uvicorn命令行联合校验，不按端口或裸 PID误杀。
- 最终 ZIP 再次解压到全新目录验证：服务在 `8080` 健康后强制结束启动器 PowerShell，Uvicorn 与端口均自动释放；与 `1.30.00` 同时运行时分别使用 `8081／8080`，本版 `stop.bat` 只停止本版服务。
# AI 修改入口（2026-07-22）

本目录新增 `AGENTS.md` 和 `1.30.10_GAEALaViC专用改动边界与迭代规则.md`。后续任何 AI 修改本版本或其增量版前，必须先阅读并遵循这两份文件：`.10` 保持冻结，现场反馈先复制为 `.11`；所有改动先分类为通用核心、GAE 专用、环境／网络／权限或未知待确认；GAE 专用常量不得进入通用 Runner；真实目标站未执行的结论保持 `unverified/blocked`。

## 37. 多网站分支规则与 GAE 专用边界固化（2026-07-22）

- 根接续说明已固定 `1.30.00` 为多网站共同母版；GAE 使用 `.10→.11→.12`，第二网站 `.20→.21→.22` 必须从 `1.30.00` 独立建立，不继承 GAE 分支。
- 版本根与交付包根均新增 `AGENTS.md` 和专用边界文档，确保组员解压后让 AI 从包根工作时仍自动受到约束。
- 已按通用能力、GAE 专用资产和企业现场待验证事实归类。当前 `acceptance` 层写死 S01～S30、5 次／150 分母、GAE 报告标题和 benchmark 根，归为 GAE 专用，不能直接复制到第二网站。
- Adapter 模板的 35 项中 24 项只是指导书候选 `configured`、11 项 blocked；S01～S30 均仍需现场绑定。真实目标站 30×5／L4 继续为 `unverified`。
- 本次仅增加开发约束、边界说明并同步封装清单，未修改业务代码、运行时、场景或企业验收结论。
- 更新后 `AI-GUI 1.30.10.zip` 大小为 `1,087,034,412` 字节，SHA-256 为 `D5578E419C0A8560A5E80B22121F8F8856F1FC3AE01A7A11ADA2558D19E8FFA5`；该值替代第 36 节及根接续说明第 52／53 节的旧交付哈希。
