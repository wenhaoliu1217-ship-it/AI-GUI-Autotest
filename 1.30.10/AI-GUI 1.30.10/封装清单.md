# AI-GUI 1.30.10 封装清单

封装日期：2026-07-22

## 完成口径

- 已依据两份 GAEALaViC 指导书完成 GUI 功能、目标站 Adapter 模板、S01～S30 运行时绑定、30×5 批次和 L4 编排。
- 随包提供 `AGENTS.md` 和 `1.30.10_GAEALaViC专用改动边界与迭代规则.md`；组员后续使用 AI 修改时必须先读取，现场反馈应建立 `.11/.12`，不得覆盖冻结的 `.10`。
- 公司内网真实运行不作为本功能包封装前置条件。
- 当前不得声明“企业目标站已验证”；真实 30×5 和至少一次真实 L4 仍待公司网络窗口。

## 验证

- 后端：`179 passed, 6 skipped`。
- 前端：`21 passed`。
- TypeScript／生产构建：通过。
- 显式 Docker 发布验收：`4 passed`。
- Docker 镜像：`sha256:e8bf519c1bef6e59baa763387a52a479652f42846f44dcc278e200095507fa11`。
- 后端同步：180 个源码／benchmark／example 文件零差异。
- 前端同步：源码、一键运行、构建成品 5 个文件 SHA-256 一致。
- 前端资源：`index-CIO-IXCF.js`、`index-CViZXObd.css`。
- 残留 `ai-gui-*` 容器：0。

## 无内网合同演练

- 30×5：`acceptance-20260722-113030-4ddc0ff2`，150/150 完成，`dry_run_only`。
- L4：`l4-20260722-113033-172d0b24`，11 阶段完成，`unverified／dry_run_only`。

## 当前服务

- 业务功能封装阶段的一键包验证地址：`http://127.0.0.1:8084/`；该历史验证服务已退出。

## 冻结版本便携交付兼容性补丁

- 补丁日期：2026-07-22。
- 修改范围仅为 `Windows一键运行` 启动层、交付说明和重新封装；未修改业务功能。
- 包内 Python：`3.12.10`，启动器不读取系统 Python 或 `py`。
- 包内浏览器：Playwright `1.49.1` 对应的 Chromium、headless shell 和 FFmpeg，固定使用包内 `PLAYWRIGHT_BROWSERS_PATH`。
- 包内 Runner：`runtime/images/ai-gui-runner-1.30.10.tar` 或同名 `.tar.gz` 离线归档。
- 启动器禁止运行时 `pip install`、`playwright install` 和 `docker build`；缺少便携组件时直接失败并要求重新解压完整包。
- Docker Desktop 已安装但 Engine 未就绪时，启动器会自动拉起并等待；不会降级为宿主机执行。
- `start.ps1` 使用 Windows Job Object `KILL_ON_JOB_CLOSE` 绑定 uvicorn 子进程，直接关闭启动窗口也会回收服务并释放端口。
- 运行期间写入 `server.pid`，正常退出自动清除；启动前清理严格核对当前包根目录、包内 Python 实际路径和 uvicorn 命令行，不按端口或裸 PID 结束进程。
- 随包提供 `stop.ps1／stop.bat` 手动停止兜底；所有权校验不一致时拒绝结束进程，确保不影响其他版本或其他 Python 任务。

## 便携启动验证

- 当前目录 `Windows一键运行/start.bat` 已成功启动，最终地址为 `http://127.0.0.1:8081/`，健康检查通过。
- 已删除本机 `ai-gui-runner:1.30.10` 后重新验证；启动器从包内 tar 成功恢复镜像。
- 恢复镜像 ID：`sha256:e8bf519c1bef6e59baa763387a52a479652f42846f44dcc278e200095507fa11`。
- 验证完成后服务已停止，退出旧容器已清理，残留 `ai-gui-*` 容器为 0。

## 最终便携 ZIP

- 文件名：`AI-GUI 1.30.10.zip`。
- 文件大小与 SHA-256：以压缩包相邻的 `AI-GUI 1.30.10.zip.sha256` 和版本状态文档为唯一外部校验记录，避免压缩包自引用导致哈希变化。
- 封装日期：2026-07-22。
- 第 34／35 节记录的精简包大小和哈希属于便携运行时加入前的历史包，不再作为本次最终交付校验值。
