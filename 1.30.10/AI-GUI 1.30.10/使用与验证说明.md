# AIGU-2.3 AI 接入版真实 GUI 自动化测试平台

更新日期：2026-07-20

## 重要变化

本版本已移除前端 Mock 结果生成器。系统现在通过本地 FastAPI 服务调用 Playwright Chromium，真正访问目标网站、执行审核后的步骤和断言，并为每个已执行步骤保存 PNG 截图。

- 未连接执行服务时，GUI 明确报错，不生成成功或失败结果。
- 已删除“制造一个失败结果”开关；最终状态来自浏览器实际执行。
- 无法解析的自然语言会返回错误或人工补充项，不会默认判定成功。
- “截图”按钮打开本次运行实际生成的 PNG 证据。
- 新增“AI 模型设置”入口，支持 OpenAI Responses API 和兼容 Chat Completions。
- AI Key 仅保留在当前网页内存并传给本机后端处理；不落盘、不进入浏览器存储、历史记录或报告，刷新页面即清除。
- 模型生成的计划必须经过封闭动作白名单和 Pydantic Schema 校验，并仍需人工审核后才能执行。
- 新增企业“项目接入”：项目配置、允许域名、运行限制和真实只读兼容性扫描。
- L1 项目可导入 Playwright `storageState`；Cookie/Origin 先做域名校验，再使用 Windows 当前用户 DPAPI 加密，明文不会写入磁盘、报告或前端响应。

## 项目接入与 L1 登录态

1. 点击左侧“项目接入”，填写项目名称、Base URL、允许域名和运行限制。
2. L0 用于公开／免登录页面；L1 用于需要已登录会话的业务页面。
3. L1 可选择由 Playwright 生成的 `storageState.json`，再点击“加密导入登录态”。
4. 系统只展示 Cookie 数量、Origin 数量、域名和过期状态，不展示 Cookie 值。
5. 点击“启动真实只读扫描”；扫描只导航和观察，不点击、不填写、不提交。
6. 选择该项目后新建并执行测试，真实浏览器会加载该项目的登录态和允许域名限制。

登录态使用 Windows DPAPI CurrentUser 加密，只能由导入它的同一 Windows 用户解密。复制到其他电脑或其他 Windows 账号后需要重新导入。产品内交互录制登录态尚未实现。

## 在网页中接入 AI

1. 启动平台后，点击左侧“AI 模型设置”。
2. OpenAI 用户保留“Responses API”和 `https://api.openai.com/v1`；兼容服务请选择“Chat Completions”并填写服务商地址。
3. 填写模型名称和重新生成、从未暴露的新 API Key。
4. 点击“测试模型连接”；成功后点击“使用 AI 新建测试”。
5. 输入自然语言流程，选择“AI 模型规划”，生成后逐步审核，再启动真实浏览器测试。

连接测试和 AI 规划会产生模型调用费用。页面不会保存配置，刷新后需重新填写。已经在聊天、截图或日志中公开过的 Key 必须撤销，不能继续使用。

## Windows 最快运行

1. 由管理员预先安装并配置 Docker Desktop、WSL 2／Virtual Machine Platform，确认 Linux Engine 可用；不需要安装系统 Python、`py`、`pip` 或 Node.js。
2. 完整解压交付 ZIP，再打开 `Windows一键运行` 文件夹；不要在压缩包预览窗口中直接运行。
3. 双击 `start.bat`。启动器固定使用包内 Python 3.12.10、固定 Python 依赖和 Playwright 1.49.1 对应的包内 Chromium。
4. 本机没有 `ai-gui-runner:1.30.10` 时，启动器从包内离线 Runner 镜像归档执行 `docker load`。
5. 启动器不会联网执行 `pip install`、`playwright install` 或 `docker build`；便携运行时缺失时会明确要求重新解压完整包。
6. 浏览器打开启动窗口显示的 `http://127.0.0.1:<端口>/` 后，页面顶部应显示“真实执行服务已连接”。

运行证据保存在：

```text
Windows一键运行/artifacts/<运行编号>/
  run.json
  events.jsonl
  report.html
  report.md
  trace.zip
  screenshots/*.png
```

## 自然语言格式

当前内置的是透明、确定性的中文规则规划器。推荐使用明确语句并用分号分隔：

```text
在“用户名”输入“admin”；
在“密码”输入“admin123”；
点击“登录”；
确认看到“客户管理”；
截图
```

复杂业务流程可在“AI 模型设置”中接入经企业批准的模型供应商。AI 只负责把文字转换为受约束计划，不会直接宣布测试成功；最终结果仍来自 Playwright 的真实操作和断言。

## 真实执行边界

- 只测试已获授权的 Web 测试环境。
- 首期执行器为 Chromium；不包含小程序或 Windows 桌面自动化。
- 验证码、MFA、SSO 和真实账号必须采用企业批准的测试方案。
- 目标页面发生跨域跳转时，执行器默认拒绝。
- 失败原因属于启发式提示，必须结合截图与 trace 人工复核。

## 公网部署说明

纯静态托管无法在云端启动 Chromium。公网部署必须同时部署 Python 执行服务到允许启动浏览器的 Windows/Linux 主机或容器，并在构建 GUI 时设置 `VITE_API_BASE_URL`。只部署 `dist` 会显示“执行服务未连接”，这是防止伪造结果的设计行为。

## 验证结果

- GAEALaViC 企业验收页支持 S01～S30 运行时绑定、固定 30×5 持久批次、进度、取消、恢复、失败重试和报告下载。
- L4 支持依赖编排、跨阶段输出、状态时间线、失败逆序清理和人工清理清单；无公司网络时可运行合同演练，结果明确标记“未实测”。
- 2026-07-22 完整回归：后端 179 通过、6 个需显式触发的验收项跳过；前端 21 通过；Docker 显式验收 4 通过；TypeScript 和生产构建通过。
- 当前功能包可按两份 GAEALaViC 指导书封装。企业目标站 30×5 和真实 L4 仍需连接公司网络后补充，不影响功能包运行。

- 前端测试：21/21 通过。
- TypeScript 类型检查：通过。
- Python/API/Playwright 测试：179/179 通过；6 项外部／Docker 验收默认跳过，其中 Docker 发布验收已显式 4/4 通过。
- AI 接口测试：连接、密钥脱敏、结构化计划校验和目标地址防篡改均通过；未使用真实 Key。
- 真实成功路径：浏览器访问本地授权演示站，完成登录、页面断言和逐步截图。
- 真实失败路径：定位不存在的按钮，返回错误终态、失败分类和截图证据。
- GUI 联调：1440px 与 390px 均完成计划生成、审核、真实执行、报告和截图打开验证。

## 2026-07-22 冻结版本便携交付补丁验证

- 本次仅修复冻结版本的 Windows 交付兼容性；未修改业务功能、验收场景或企业目标站验证结论。
- 便携运行时固定为 Python 3.12.10；Playwright 1.49.1、对应 Chromium／headless shell／FFmpeg 均位于交付包内。
- Runner 镜像以离线归档随包交付。删除本机 `ai-gui-runner:1.30.10` 后重新双击启动，启动器已从归档恢复镜像；恢复后的镜像 ID 为 `sha256:e8bf519c1bef6e59baa763387a52a479652f42846f44dcc278e200095507fa11`。
- 当前 `Windows一键运行` 目录中的 `start.bat` 已实际启动到 `http://127.0.0.1:8081/`，健康检查通过。
- 验证结束后已停止本次服务并清理退出旧的 `ai-gui-*` 容器，没有保留测试容器。
- 启动器把 uvicorn 子进程加入带 `KILL_ON_JOB_CLOSE` 的 Windows Job Object；正常按 Enter 或直接关闭启动窗口都会回收同包服务并释放端口。
- 运行期间生成 `server.pid`，记录进程号、包根目录、便携 Python 路径和端口；正常退出后自动清除。再次启动只会在实际 Python 路径和 uvicorn 命令行均属于当前包时清理旧 PID 对应进程。
- 新增 `Windows一键运行/stop.bat` 作为手动停止兜底。它不按端口杀进程；PID 元数据、实际可执行路径或命令行不一致时会拒绝停止，不影响其他版本和其他程序。
- 最终 ZIP 大小和 SHA-256 以便携运行时、浏览器和离线镜像全部写入后的封装清单及相邻 `.sha256` 文件为准。
